"""سقف طول فیلدهای متنیِ آزاد (P2-05).

تا پیش از این، شواهد و کامنت‌ها و شرح‌ها هیچ سقفی نداشتند: نه در schema و نه در
ستون. یعنی یک کاربرِ احراز هویت‌شده می‌توانست چند مگابایت متن در ردیفی بنویسد که
در *هر* نمایش پرونده خوانده می‌شود و در هر خروجی Excel و هر PDF تکرار می‌شود.

اعداد این‌جا از روی کارِ واقعی انتخاب شده‌اند، نه از روی محدودیت فنی:

* شواهدِ یک شاخص یک بند توضیح است، نه یک گزارش — ۲۰۰۰ نویسه حدود یک صفحهٔ A4 است.
* کامنت گفت‌وگوی زنجیره است و ممکن است نقل‌قول داشته باشد — دو برابر.
* دلیلِ برگشت/لغو/واگذاری یک جملهٔ تصمیم است — ۱۰۰۰.
* شرح شاخص در فرم ارزیابی چاپ می‌شود و باید در یک سطر جدول جا شود.

هر مقدار در دو جا استفاده می‌شود و باید یکی بماند: `Field(max_length=...)` در
schema (که خطای کاربرپسند می‌دهد) و `String(...)` روی ستون (که هر مسیر نوشتنِ
دیگری — seeder، ورودی اکسل، SQL مستقیم — را هم می‌بندد). تستِ
test_text_limits.py این هم‌ترازی را می‌سنجد تا با گذشت زمان از هم جدا نشوند.
"""

# شواهدِ یک شاخص، و یادداشت خودارزیابی روی یک شاخص
EVIDENCE_MAX = 2000
SELF_ASSESSMENT_NOTE_MAX = 1000
# یادداشت کلی خودارزیابی (یکی برای کل پرونده، نه هر شاخص)
SELF_ASSESSMENT_SUMMARY_MAX = 2000

# گفت‌وگوی زنجیره روی پرونده
COMMENT_MAX = 4000
# جمع‌بندی ارزیاب روی کل پرونده
EVALUATOR_COMMENT_MAX = 4000

# دلیلِ یک تصمیم: برگشت، لغو، واگذاری HR، جابه‌جایی مسئول مرحله
REASON_MAX = 1000

# اعتراض کارمند و پاسخ منابع انسانی — هر دو سند رسمی‌اند
OBJECTION_MAX = 2000

# دلیلِ امتیاز ویژه — شرحِ همان یک کارِ خارج از شرح وظایف، نه یک گزارش
BONUS_REASON_MIN = 10
BONUS_REASON_MAX = 500

# برنامهٔ بهبود
PLAN_TITLE_MAX = 200
PLAN_SUMMARY_MAX = 4000
PLAN_GOAL_MAX = 1000

# شاخص‌های فرم ارزیابی (نوشتنی توسط منابع انسانی، چاپ‌شونده در فرم)
INDICATOR_CATEGORY_MAX = 200
INDICATOR_DESCRIPTION_MAX = 1000

# علتِ شکست ارسال، همان‌طور که سرویس بیرونی گفته. سقف لازم است چون این متن از
# بیرون می‌آید و یک سرویس بدرفتار می‌تواند صفحه‌ها HTML برگرداند.
DELIVERY_ERROR_MAX = 500

#: سقف‌هایی که هیچ فرمی ندارند — متنشان از سرویسِ بیرونی می‌آید، نه از دستِ
#: کاربر. بقیهٔ ثابت‌های این ماژول در `frontend/src/utils/textLimits.ts` آینه
#: می‌شوند تا فرم پیش از «ثبت» سقف را نشان بدهد، و
#: `test_text_limits.py::test_the_frontend_mirrors_every_limit` نمی‌گذارد دو
#: طرف از هم جدا بیفتند. این فهرست، همان تست را از این چند تا معاف می‌کند.
NOT_USER_FACING = ("DELIVERY_ERROR_MAX",)
