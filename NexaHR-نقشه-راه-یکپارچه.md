# نقشه راه یکپارچهٔ NexaHR V2
### ترکیب ۸ ممیزی مستقل (GLM 5.2 Max، Opus 5-1، Sonnet 5، Enterprise/GSO، Kimi، audit، nexahr_audit_gso، opus5n) روی ریپازیتوری `dbsgraphicir-max/DbsPulse_V2`

> این سند حاصل تطبیق و یکپارچه‌سازی هشت گزارش ممیزی مستقل و کاملاً جداگانه است که همگی یک ریپازیتوری را — با ابزارها و زوایای دید متفاوت — بررسی کرده‌اند. هر جا هشت گزارش به یک نتیجه رسیده‌اند، آن یافته با اطمینان بالا آورده شده؛ هر جا اختلاف‌نظر بوده، هر دو دیدگاه ذکر شده است. اعداد امتیاز، میانگین وزنی هشت گزارش‌اند.

---

## ۰. جمع‌بندی یک‌خطی

NexaHR یک **موتور گردش‌کار واقعاً مهندسی‌شده** برای تصمیم تمدید قرارداد پرسنل است (زنجیرهٔ تأیید مسئول واحد ← منابع‌انسانی ← معاونت ← مدیرعامل)، با state machine سمت سرور، قفل ردیف در دیتابیس، هش Argon2، رفرش‌توکن چرخشی، و کارنامهٔ نهایی قابل تأیید با QR+SHA-256. این هستهٔ فنی از اکثر «فرم‌سازهای» بازار HR ایرانی جدی‌تر است.

اما در حال حاضر یک **پلتفرم مدیریت عملکرد کامل** نیست (بدون هدف‌گذاری/OKR، بدون خودارزیابی، بدون کالیبراسیون) و برای فروش تجاری به مشتری دوم، **آمادهٔ عملیاتی‌شدن** نیست: بدون بکاپ/DR، بدون MFA برای نقش‌های تصمیم‌گیرندهٔ قرارداد، حساب‌های دمو با رمز مشترک بدون گارد پروداکشن، بدون observability، و بدون معماری چندمستأجری (multi-tenant).

**امتیاز کلی ترکیبی (میانگین وزنی ۸ گزارش): ۵٫۸ – ۶٫۱ از ۱۰**
**آمادگی تجاری‌سازی/فروش (Commercial Readiness): ۱٫۷ – ۳ از ۱۰** — فاصلهٔ معنادار بین «کد خوب» و «محصول قابل‌فروش».

---

## ۱. جدول امتیاز ترکیبی (میانگین ۸ گزارش)

| حوزه | وزن پیشنهادی | امتیاز میانگین | تفسیر |
|---|---|---|---|
| بلوغ قابلیت محصول (Product Capability) | ۲۵٪ | ۵٫۵ – ۶٫۰ | هستهٔ ارزیابی کامل است؛ لایه‌های مدیریت عملکرد مدرن (هدف‌گذاری، کالیبراسیون، خودارزیابی) غایب‌اند |
| صحت و اجرای گردش‌کار (Workflow Integrity) | ۲۰٪ | ۷٫۵ – ۸٫۳ | قوی‌ترین بخش پروژه — واقعاً در بک‌اند اجرا می‌شود، نه فقط در UI |
| امنیت و کنترل دسترسی (Security) | ۱۵٪ | ۶٫۵ – ۷٫۶ | پایه‌های خوب (Argon2، رفرش‌چرخشی، RBAC) اما فاقد MFA و rate-limit توزیع‌شده |
| آمادگی سازمانی/عملیات (Enterprise & Ops) | ۱۵٪ | ۵٫۰ – ۵٫۴ | ضعیف‌ترین بخش — بدون بکاپ، بدون observability، بدون HA |
| تجربهٔ کاربری (UX) | ۱۰٪ | ۶٫۰ – ۷٫۰ | فارسی/RTL درجه‌یک، اما دسکتاپ‌محور و بدون موبایل |
| کیفیت تست و مهندسی (QA) | ۱۰٪ | ۶٫۵ – ۸٫۰ | تست‌های بک‌اند واقعی و در تراکنش rollback؛ بدون E2E و بدون gate پوشش |
| آمادگی استقرار (Deployment) | ۵٪ | ۵٫۰ – ۶٫۵ | Docker Compose خوب برای تک‌نمونه؛ فاقد zero-downtime و IaC |
| **میانگین کلی** | ۱۰۰٪ | **≈ ۵٫۸ – ۶٫۱ / ۱۰** | محصول با هستهٔ معتبر، پوستهٔ تجاری ناقص |
| آمادگی تجاری‌سازی (جداگانه) | — | **۱٫۷ – ۳٫۰ / ۱۰** | بدون لایسنس، بدون چندمستأجری، بدون بسته‌بندی برای مشتری دوم |

---

## ۲. رجیستری یکپارچهٔ یافته‌ها (Consolidated Findings)

یافته‌های ۸ گزارش با هم تطبیق و تکراری‌ها حذف شدند. هرکدام برچسب **«اجماع»** (در اکثر یا همهٔ گزارش‌ها تکرار شده) یا **«تک‌گزارشی»** (فقط در یک یا دو گزارش دیده شده، اما قابل اعتنا) دارند.

### 🔴 P0 — موانع بحرانی پیش از هرگونه استقرار واقعی (اجماع کامل ۸/۸ گزارش)

| # | یافته | شواهد / محل | راه‌حل |
|---|---|---|---|
| P0-1 | **حساب‌های دمو با رمز مشترک** (`NexaHR@12345`) بدون هیچ گارد در پروداکشن — در تمام محیط‌ها seed می‌شوند | Migration/seed data | Seed دمو را پشت یک فلگ محیطی ببرید؛ در `ENVIRONMENT=production` کاملاً غیرفعال یا اجباراً حذف شود؛ CLI برای ساخت ادمین واقعی اضافه کنید |
| P0-2 | **فقدان کامل بکاپ/بازیابی/DR** — فایل‌های PDF نهایی فقط در ستون bytea دیتابیس زندگی می‌کنند؛ هیچ RPO/RTO مستندی وجود ندارد | بدون آرتیفکت بکاپ در ریپو | Job بکاپ زمان‌بندی‌شده + تست واقعی Restore + انتقال PDF/اسناد به Object Storage (S3-compatible) با نگهداری نسخه |
| P0-3 | **audit log جدول قابل‌تغییر است، نه tamper-evident** — هیچ hash-chain یا مکانیزم غیرقابل‌دستکاری ندارد؛ برای انطباق HR/حقوقی خطرناک است | جدول ساده در دیتابیس | Hash-chain روی رکوردهای لاگ، لغو دسترسی UPDATE/DELETE در سطح دیتابیس، خروجی امضاشدهٔ خارجی |
| P0-4 | **Rate limiting فقط حافظهٔ درون‌پردازه‌ای (`slowapi`) و فقط روی لاگین/رفرش** — با چند replica به‌اشتراک گذاشته نمی‌شود و به‌سادگی دور زده می‌شود؛ endpointهای سنگین (گزارش‌ها، اکسل) بدون محدودیت‌اند | `slowapi` in-memory | انتقال به Redis-backed rate limiter؛ پوشش endpointهای سنگین؛ `FORWARDED_ALLOW_IPS` را محدود به شبکهٔ پروکسی کنید |
| P0-5 | **بدون MFA برای نقش‌های تصمیم‌گیرندهٔ قرارداد** (معاونت، مدیرعامل) — امضای نهایی تمدید/عدم‌تمدید قرارداد بدون لایهٔ دوم احراز هویت | — | حداقل TOTP اختیاری/اجباری برای نقش CEO/Deputy پیش از تأیید نهایی |
| P0-6 | **بدون اسکن آسیب‌پذیری وابستگی‌ها/کانتینر و بدون SBOM** — هیچ Dependabot/Trivy/Snyk در CI | `.github/workflows/ci.yml` | افزودن dependency scan + container scan + SBOM به CI؛ انتشار `SECURITY.md` |
| P0-7 | **رندر PDF همزمان (synchronous) داخل worker وب** و در صورت خطا **بی‌صدا شکست می‌خورد** — با WeasyPrint سنگین، ریسک بلوکه‌شدن و از دست رفتن خروجی رسمی | تولید هم‌زمان درخواست | صف‌بندی async (Celery/RQ/Arq)، وضعیت سند صریح (pending/done/failed)، retry و reconciliation |
| P0-8 | **بدون observability** — نه متریک، نه tracing، نه alerting؛ فقط لاگ stdout | — | Structured logging + متریک (Prometheus/OpenTelemetry) + error tracking (Sentry) + حداقل هشدارهای بحرانی |
| P0-9 | **پیکربندی پروداکشن روی مقادیر پیش‌فرض ناامن fail-open می‌شود / کانفیگ گم‌شده باعث شکست نامشخص می‌گردد** — برخی گزارش‌ها fail-closed بودن این مسیر را تأیید می‌کنند اما نمونهٔ env تولیدی رسمی موجود نیست | `docker-compose.yml` | انتشار `.env.production.example` رسمی + Fail-fast صریح روی JWT_SECRET/CORS پیش‌فرض (بخشی از این در کد فعلی وجود دارد، باید تکمیل و مستند شود) |
| P0-10 *(تک‌گزارشی، اما جدی)* | **امکان تداخل هویت ارزیاب/ارزیابی‌شونده** — یک مدیر می‌تواند به‌صورت نظری دسترسی ارزیابی روی خودش داشته باشد | لایهٔ `evaluation_access` | افزودن CHECK constraint در دیتابیس + اعتبارسنجی کد که مانع تخصیص ارزیاب = ارزیابی‌شونده شود |
| P0-11 *(تک‌گزارشی)* | **اجرای خودکار Migration در استارت‌آپ کانتینر** — در استقرار چندنسخه‌ای (multi-replica) ریسک رقابت (race condition) دارد | Entrypoint | جدا کردن مرحلهٔ migration از استارت‌آپ اپ (job جداگانه پیش از rollout) |
| P0-12 *(اجماع نسبی)* | **فقدان معماری چندمستأجری (Single-org data model)** — فروش به مشتری دوم بدون فورک کد یا نمونهٔ جداگانه ممکن نیست | Schema | تصمیم استراتژیک: یا Deploy-per-customer رسمی (Wave 2) یا بازطراحی چندمستأجری (Wave 4) |

### 🟠 P1 — الزامات رقابت تجاری معتبر (اجماع بالا، ۶–۸ از ۸ گزارش)

| # | یافته | راه‌حل خلاصه |
|---|---|---|
| P1-1 | بدون خودارزیابی کارمند — چرخه فقط از مدیر شروع می‌شود | افزودن مرحلهٔ خودارزیابی پیش از ارسال مدیر، با نمایش مقایسه‌ای |
| P1-2 | بدون کالیبراسیون/نرمال‌سازی بین واحدها | Workspace کالیبراسیون با نمای توزیع امتیاز و ثبت دلیل تعدیل |
| P1-3 | وزن‌ها/آستانه‌ها/زنجیرهٔ ۴مرحله‌ای کاملاً hard-coded در کد | جدول‌سازی قالب‌های شاخص/وزن/آستانه با تاریخ اثر (effective date)، بدون نیاز به دیپلوی مجدد |
| P1-4 | بدون یکپارچگی خارجی: API عمومی، Webhook، اتصال HRIS، ورود گروهی CSV | نسخه‌بندی API عمومی + Webhook امضاشده + CLI Import پرسنل |
| P1-5 | بدون MFA عمومی و بدون محدودیت تلاش ورود فراتر از rate-limit پایه | Lockout پس از تلاش‌های ناموفق + TOTP اختیاری برای همهٔ نقش‌ها |
| P1-6 | یادآوری مهلت/SLA فقط تزئینی است — هیچ تشدید (escalation) یا مسدودسازی واقعی رخ نمی‌دهد | State machine مهلت‌دار با escalation خودکار به نقش بالاتر پس از انقضا |
| P1-7 | بدون جانشین‌گذاری/تفویض برای تأییدکنندهٔ غایب | مکانیزم Delegation موقت با ثبت audit |
| P1-8 | اعلان‌ها فقط درون‌برنامه‌ای‌اند — اگر کاربر لاگین نکند، اعلان هرگز دیده نمی‌شود | افزودن کانال ایمیل/پیامک با ترجیحات و خلاصهٔ روزانه/هفتگی |
| P1-9 | بدون مکانیزم لایسنس/محدودیت seat/انقضا/بسته‌بندی پلن | لایهٔ Licensing (seat count, expiry, plan gating) |
| P1-10 | مستندسازی در سطح توسعه‌دهنده است، نه سطح تدارکات سازمانی (procurement-grade) | بستهٔ امنیتی/حریم‌خصوصی/معماری برای پاسخ به پرسشنامهٔ خرید سازمانی |
| P1-11 | بدون قالب حقوقی: ToS/DPA/SLA؛ خود ریپوی عمومی بدون لایسنس نرم‌افزاری | افزودن LICENSE + قراردادهای نمونهٔ ToS/DPA/SLA |
| P1-12 | CSRF روی کوکی رفرش فقط به `SameSite=strict` متکی است، بدون توکن CSRF مجزا | افزودن CSRF token صریح برای عملیات state-changing |
| P1-13 | زمان‌بند (scheduler) درون‌پردازه‌ای، به‌صورت پیش‌فرض غیرفعال و در چندنسخه‌ای تکراری اجرا می‌شود | Scheduler پایدار با leader election و تاریخچهٔ اجرا (مثلاً APScheduler+DB lock یا Celery beat) |
| P1-14 | تشخیص توقف SLA بر اساس `created_at` است، نه زمان ورود به مرحلهٔ فعلی | ثبت timestamp ورود به هر مرحله جداگانه |
| P1-15 | بدون ابزار Provisioning/Onboarding برای مشتری جدید (نصب دستی و یکتا) | CLI راه‌اندازی اولیه + Wizard first-run + Import سازمانی |
| P1-16 | بدون گردش‌کار نگهداری داده/صادرات/حذف قانونی (Data Retention/Erasure) | سیاست نگهداری + Subject Access Export + Erasure حسابرسی‌شده |
| P1-17 | بدون ۳۶۰ درجه/بازخورد همکاران | ماژول بازخورد چندجانبه با آستانهٔ ناشناس‌ماندن |
| P1-18 | Concurrency خوش‌بینانه (Optimistic Concurrency) روی ویرایش امتیاز وجود ندارد | نسخه‌بندی ردیف (version token) برای جلوگیری از هم‌پوشانی ویرایش |

### 🟡 P2 — بهبودهای پرارزش (اجماع متوسط)

- بدون ماژول هدف‌گذاری/OKR — سیستم فقط ارزیابی می‌کند، مدیریت عملکرد مستمر نمی‌کند
- چارچوب شایستگی (Competency) تخت و تک‌سطحی، بدون خانوادهٔ شغلی
- بدون e2e تست مرورگری (Playwright/Cypress)، بدون coverage gate در CI
- تجربهٔ کارمند فقط یک صفحهٔ فقط‌خواندنی؛ بدون سطح موبایل/PWA
- endpoint تأیید عمومی اطلاعات بیش‌ازحد (نام+واحد+امتیاز) به هر دارندهٔ توکن نشان می‌دهد
- ویژگی «دوره‌های ارزیابی» پیاده شده ولی پشت فلگ hard-coded دارک‌لانچ مانده
- بدون escalation/فراخوانی HR برای پرونده‌های گیرکرده — بدون راه فرار
- `FORWARDED_ALLOW_IPS` پیش‌فرض `*` است — ریسک دور زدن rate-limit با هدر جعلی
- تصمیم تمدید/عدم‌تمدید هرگز به‌صورت رسمی رکورد نمی‌شود (implied نه explicit)
- بدون پیوست شواهد (فایل/عکس) برای امتیازهای حدی
- برندینگ در کد hard-code شده — حتی ممکن است نام مشتری اصلی در برند نمایان بماند

### 🟢 P3 — تمایزدهنده‌های قوی (در صورت تکمیل P0/P1)

- Renewal-Risk Radar — رادار پیش‌بینی ریسک عدم‌تمدید ۹۰ روز پیش از موعد
- بج صحت امتیاز و آنالیتیکس رفتار ارزیاب (rater analytics) به‌صورت محصول
- White-label و تم‌سازی runtime برای هر مستأجر
- SSO/SCIM (OIDC/SAML)
- تحلیل روند/کوهورت/توزیع در طول زمان (Longitudinal analytics)
- ارزیابی مستمر (چک-این‌های سبک، ۱:۱)

### 🚀 Moonshots — با احتیاط و گاردریل

- **دستیار نگارش شواهد فارسی با AI** — فقط پیش‌نویس متن، هرگز امتیازدهی؛ خروجی برچسب‌گذاری‌شده به‌عنوان AI-generated؛ قابل غیرفعال‌سازی
- **معماری SaaS چندمستأجری کامل**
- **شبکهٔ محک صنعتی ناشناس‌سازی‌شده برای بنگاه‌های ایرانی**
- **دفتر ثبت اعتبارنامهٔ قابل‌تأیید** — امتداد QR+SHA-256 فعلی به یک رجیستری امضاشدهٔ خارجی، برای پرتابل‌بودن سابقهٔ ارزیابی کارمند بین کارفرمایان
- **ممیزی آماری سوگیری ارزیاب** — تحلیل مداوم رفتار ارزیابان (سخت‌گیری/سهل‌گیری/فشردگی امتیاز) با گزارش انصاف قابل دفاع در برابر نهاد نظارتی
- **دستیار فارسی درون‌سازمانی (on-prem)** — مدل زبانی محلی که کیفیت بازخورد مدیران را کوچ می‌کند بدون خروج داده از شبکهٔ مشتری — تنها گزینهٔ AI که با حساسیت حاکمیت داده در ایران سازگار است

---

## ۳. نقشهٔ راه فازبندی‌شده (موج‌های اجرا)

> **قانون طلایی هر ۸ گزارش:** هیچ ویژگی جدید (Wave 3/4) را روی زیرساختی که نمی‌توان بکاپ گرفت، دیباگ کرد یا امن‌سازی کرد اضافه نکنید. عمق ویژگی روی پایهٔ ناپایدار، ریسک را افزایش می‌دهد نه ارزش را.

### 🌊 موج ۰ — «جلوی خونریزی را بگیر» (۱–۲ هفته)

هیچ‌چیز به مشتری واقعی عرضه نمی‌شود تا این‌ها بسته شوند:

1. حساب‌های دمو را پشت فلگ محیطی ببرید و در پروداکشن رد کنید؛ CLI ساخت ادمین واقعی اضافه کنید *(P0-1)*
2. فلگ «تغییر اجباری رمز عبور» را واقعاً در dependency احراز هویت اعمال کنید *(P0-1 مرتبط)*
3. تداخل هویت ارزیاب/ارزیابی‌شونده را در کد و با CHECK constraint دیتابیس مسدود کنید *(P0-10)*
4. روی سکرت‌های پیش‌فرض/گم‌شده fail-closed شوید؛ `.env.production.example` رسمی منتشر کنید *(P0-9)*

**نتیجهٔ این موج:** استقرار برای مشتری واقعی از نظر بنیادی «امن» می‌شود.

### 🌊 موج ۱ — «قابل بهره‌برداری در پروداکشن» (۳–۴ هفته)

بازیابی، دیدپذیری و پاسخ امنیتی قابل دفاع:

1. Job بکاپ + تمرین واقعی Restore + RPO/RTO مستند + انتقال اسناد به Object Storage *(P0-2)*
2. Hash-chain کردن audit log، لغو UPDATE/DELETE، خروجی امضاشدهٔ خارجی *(P0-3)*
3. آرشیو PDF پایدار و retry-پذیر با وضعیت سند صریح و reconciliation *(P0-7)*
4. لاگ ساخت‌یافته + متریک + Error tracking + مجموعهٔ حداقلی هشدار *(P0-8)*
5. اسکن وابستگی، SBOM و container scan در CI؛ انتشار `SECURITY.md` *(P0-6)*
6. انتقال Rate limiting به Redis؛ پوشش endpointهای سنگین؛ محدود کردن `FORWARDED_ALLOW_IPS` *(P0-4)*

**نتیجهٔ این موج:** می‌توانید بازیابی را وعده دهید، به پرسشنامهٔ امنیتی خریدار پاسخ دهید، و در پروداکشن دیباگ کنید.

### 🌊 موج ۲ — «قابل فروش به مشتری دوم» (۵–۷ هفته)

استقرار تکرارپذیر بدون فورک کد:

1. برندینگ و White-label مبتنی بر دیتابیس با پیش‌نمایش ادمین
2. CLI راه‌اندازی اولیه + Import سازمانی CSV + Wizard first-run
3. Scheduler پایدار با leader election و تاریخچهٔ اجرا
4. کانال‌های ایمیل/پیامک برای اعلان با ترجیحات و خلاصه
5. Lockout ورود + MFA اختیاری (TOTP) — حداقل برای معاونت/مدیرعامل *(P0-5, P1-5)*
6. توکن Concurrency خوش‌بینانه روی ویرایش امتیاز *(P1-18)*
7. بستهٔ تدارکات: نمای امنیتی، نمودار جریان داده، سیاست نگهداری، Runbook، تعریف SLA *(P1-10, P1-11)*
8. جدا کردن migration از استارت‌آپ کانتینر برای rollout چندنسخه‌ای *(P0-11)*

**نتیجهٔ این موج:** استقرار تکرارپذیر بدون فورک کد؛ MFA و provisioning پاسخ‌گوی بازبینی IT سازمان خریدار است.

### 🌊 موج ۳ — «همتراز با رقبا» (۸–۱۲ هفته)

عبور موفق از مقایسهٔ ویژگی به ویژگی با Lattice/Culture Amp/Leapsome/15Five:

1. قالب‌های شایستگی/وزن/آستانهٔ قابل‌پیکربندی با تاریخ اثر (بدون نیاز به دیپلوی) *(P1-3)*
2. مرحلهٔ خودارزیابی کارمند با نمای مقایسه‌ای *(P1-1)*
3. Workspace کالیبراسیون با نمای توزیع و ثبت دلیل تعدیل *(P1-2)*
4. پیوست شواهد (فایل) با اسکن ویروس و لینک امضاشده
5. API عمومی نسخه‌بندی‌شده + حساب سرویس + Webhook امضاشده *(P1-4)*
6. گردش‌کار نگهداری/صادرات/حذف قانونی داده *(P1-16)*
7. Coverage gate واقعی، تست‌های concurrency، و benchmark بار (load test)
8. escalation و مسیر فراخوانی HR برای پرونده‌های گیرکرده
9. رفع اکسپوژر بیش‌ازحد endpoint تأیید عمومی

**نتیجهٔ این موج:** محصول در مقایسهٔ رودررو با سوییت‌های میان‌بازار مدیریت عملکرد کم نمی‌آورد.

### 🌊 موج ۴ — «تمایز و رهبری بازار» (مستمر)

رهبری در بازار فارسی‌زبان:

1. تصمیم استراتژیک و اجرای چندمستأجری در صورت انتخاب مسیر SaaS *(P0-12)* — «قبل تصمیم بگیرید، نه در حین کار»
2. هدف‌گذاری/OKR که در snapshot ارزیابی جریان می‌یابد
3. بازخورد ۳۶۰ درجه با آستانهٔ ناشناس‌ماندن
4. SSO/SCIM (OIDC/SAML)
5. آنالیتیکس روند/کوهورت/توزیع با تحویل زمان‌بندی‌شده
6. صف بازبینی تشخیص ریسک: سهل‌گیری ارزیاب، فشردگی امتیاز، اثر هاله‌ای، نوسان سال‌به‌سال
7. **Renewal-Risk Radar** — پیش‌بینی ۹۰روزهٔ ریسک قرارداد با فاکتورهای قابل‌توضیح
8. PWA موبایل‌محور برای تأییدکنندگان با push notification
9. دستیار AI فارسی برای پیش‌نویس شواهد و بهبود کیفیت زبان — با گاردریل «هرگز امتیاز نمی‌دهد، همیشه برچسب‌گذاری می‌شود، همیشه غیرفعال‌پذیر است»

| موج | اولویت | برآورد زمان | نقطهٔ عطف باز‌شده |
|---|---|---|---|
| موج ۰ | P0 | ۱–۲ هفته | اصلاً امن است برای استقرار مشتری واقعی |
| موج ۱ | P0/P1 | ۳–۴ هفته | بازیابی وعده‌داده‌شدنی، پاسخ به پرسشنامهٔ امنیتی، دیباگ‌پذیری |
| موج ۲ | P1 | ۵–۷ هفته | استقرار تکرارپذیر بدون فورک؛ MFA و provisioning |
| موج ۳ | P2 | ۸–۱۲ هفته | همتراز با سوییت‌های میان‌بازار |
| موج ۴ | P3 + Moonshot | مستمر | رهبری دستهٔ محصول در بازار فارسی |

---

## ۴. نقشهٔ وابستگی‌ها (Dependency Map)

```
موج ۰ (امنیت پایه)
   │
   ├─▶ موج ۱ (بکاپ، observability، امنیت زنجیرهٔ تأمین)
   │        │
   │        ├─▶ موج ۲ (provisioning، white-label، MFA، تدارکات)
   │        │        │
   │        │        ├─▶ موج ۳ (کالیبراسیون، خودارزیابی، API عمومی)
   │        │        │        │
   │        │        │        └─▶ موج ۴ (چندمستأجری، ۳۶۰، AI، رادار ریسک)
   │        │        │
   │        │        └─▶ تصمیم چندمستأجری باید همین‌جا گرفته شود
   │        │             (نه در حین ساخت موج ۴)
   │        │
   │        └─▶ بستهٔ تدارکات به بکاپ/observability/SBOM موج ۱ وابسته است
   │
   └─▶ بدون این موج، هیچ مشتری واقعی نباید داده تولید کند
```

**وابستگی‌های حیاتی که نباید نادیده گرفته شوند:**
- تصمیم «تک‌مستأجری در برابر چندمستأجری» باید **پیش از** موج ۲ گرفته شود، وگرنه بازطراحی هزینهٔ دوباره‌کاری بالایی دارد.
- کالیبراسیون (موج ۳) بدون خودارزیابی (موج ۳) و بدون داده‌های تاریخی ارزش کمی دارد — این دو باید هم‌زمان طراحی شوند.
- Renewal-Risk Radar (موج ۴) به دادهٔ تاریخی چند دورهٔ ارزیابی و رکورد رسمی تصمیم تمدید (که هنوز رسمی ثبت نمی‌شود — یافتهٔ P2) نیاز دارد؛ ابتدا آن رکورد را رسمی کنید.
- ماژول AI (Moonshot) نباید پیش از تکمیل audit-log تمپر-اویدنت (P0-3) عرضه شود، چون خروجی AI باید در زنجیرهٔ شواهد قابل ردیابی و اثبات‌پذیر باشد.

---

## ۵. چگونه NexaHR می‌تواند برنده شود (خلاصهٔ استراتژیک)

نقاط اتفاق‌نظر ۸ گزارش دربارهٔ فرصت‌های واقعی تمایز (نه تزئینی):

1. **تمرکز فارسی‌محور و تمدید‌قرارداد‌محور همین حالا یک مزیت رقابتی واقعی است** — رقبای بین‌المللی (Lattice، Culture Amp، Workday) بومی‌سازی فارسی/RTL و منطق تمدید قرارداد ایرانی را ندارند.
2. **گردش‌کار سریع‌تر ارزیابی** — «بازبینی پنج‌دقیقه‌ای مدیر فارسی» به‌عنوان یک تجربهٔ محصولی متمایز.
3. **بینش عملکردی مبتنی بر AI، اما واقعی نه تبلیغاتی** — دستیار نگارش شواهد، نه امتیازدهی خودکار.
4. **تشخیص زودهنگام ریسک پیش از تمدید قرارداد** — Renewal-Risk Radar، بر پایهٔ داده‌ای که سیستم همین حالا جمع می‌کند.
5. **برنامه‌های بهبود هوشمندتر** — پیوند PIP به اهداف و سنجش اثربخشی.
6. **تحلیل و داشبورد اجرایی بهتر** — روند، کوهورت، پیش‌بینی.
7. **API و یکپارچگی قوی** — دروازهٔ ورود به بازار سازمانی.
8. **تجربهٔ کارمند موبایل‌محور** — سهم قابل توجهی از کاربران را کارمند عادی تشکیل می‌دهد، نه فقط تأییدکننده.
9. **پیکربندی منعطف بدون نیاز به توسعه‌دهنده** — no-code weight/threshold/template editor.

⚠️ **ریسک‌های AI که هر ۸ گزارش هشدار داده‌اند:** AI هرگز نباید امتیاز نهایی بدهد یا تصمیم تمدید را جایگزین انسان کند؛ خروجی همیشه باید برچسب «تولیدشده با AI» بگیرد؛ حساسیت حاکمیت داده در ایران به‌شدت به نفع مدل on-prem/محلی است، نه API خارجی.

---

## ۶. توصیهٔ نهایی

مسیر ارزش بالا از دید همهٔ ۸ ممیزی این ترتیب است:

1. سیستم را **امن و قابل‌بازیابی** کنید (موج ۰–۱).
2. تصمیمات نهایی و شواهد ممیزی را **غیرقابل‌دستکاری** کنید (P0-3).
3. **حق کامل کارمند** (خودارزیابی، مهلت واقعی، مسیر اعتراض) را تکمیل کنید (موج ۳).
4. گردش‌کار ثابت فعلی را به **محصول قابل‌پیکربندی** تبدیل کنید (P1-3).
5. **کالیبراسیون، هدف‌گذاری و شایستگی** اضافه کنید (موج ۳–۴).
6. بازار را با **هوش ریسک تمدید فارسی‌محور** رهبری کنید (موج ۴).

پس از تکمیل P0 و بخش اصلی P1، NexaHR می‌تواند به‌طور معتبر به‌عنوان یک پلتفرم تخصصی مدیریت عملکرد و تمدید قرارداد تک‌مستأجری در بازار ایران رقابت کند. **بدون این کنترل‌ها، باید در حد یک پایلوت کنترل‌شده باقی بماند، نه یک سیستم HRM سازمانی معرفی شود.**

---

*این سند از ترکیب و تطبیق دستی هشت گزارش ممیزی مستقل (GLM 5.2 Max، Claude Opus 5-1، Claude Sonnet 5، گزارش Enterprise/GSO، Kimi، و سه گزارش دیگر) تهیه شده است. یافته‌های تک‌گزارشی به همان صورت علامت‌گذاری شده‌اند تا سطح اطمینان مشخص باشد. این سند جایگزین بازبینی حقوقی، امنیتی یا نفوذ (Pentest) رسمی نیست.*
