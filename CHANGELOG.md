# تغییرات NexaHR

این فایل به زبان تصمیم نوشته شده، نه به زبان فنی. برای هر مورد سه چیز آمده:
**چه چیزی عوض شد**، **قبلش چه بود**، و **چرا اهمیت داشت**.

هر کار تازه‌ای که انجام شود همین‌جا اضافه می‌شود.

> شناسه‌هایی مثل `P0-03` به گزارش‌های ممیزی اشاره می‌کنند و برای پیگیری فنی‌اند؛
> برای خواندن این متن لازم نیستند. این گزارش‌ها در پوشهٔ `audit/` بودند و در
> نسخهٔ ۱.۱.۰ حذف شدند، چون ممیزیِ ریپازیتوری قدیمی `DbsPulse_V2` را توصیف
> می‌کردند. همین شناسه‌ها هنوز در کامنت‌های کد هستند؛ برای دیدن متنشان:
> `git show 7a08187:audit/00-index.md` (یا هر فایل دیگری از آن پوشه).

---

## v1.1.0 — شهریور ۱۴۰۵ (تغییر نام محصول به NexaHR)

### رابطِ همکار بازطراحی شد

**چه چیزی عوض شد** پاسخِ همکار دیگر داخلِ حبابِ باریک نیست: روی خودِ صفحه جاری
می‌شود و نشانِ فرستنده مرزش را نشان می‌دهد. پرسشِ کاربر حبابِ خودش را نگه
می‌دارد. ورودی یک ظرفِ یکپارچه شد که با نوشتن بالا می‌آید، صفحهٔ خوش‌آمد
پیشنهادها را دوستونی و با نشانه نشان می‌دهد، و نقطهٔ وضعیت می‌گوید همکار در
دسترس هست یا نه.

**قبلش چه بود** هر دو طرفِ گفت‌وگو حبابِ هم‌شکل داشتند و ورودی سه کنترلِ
جدا از هم بود.

**چرا اهمیت داشت** پاسخ‌های همکار کوتاه نیستند — جدول و فهرست و گزارش می‌آیند —
و حبابِ باریک، جدولِ چندستونی را به یک ستونِ فشرده تبدیل می‌کرد. نشانِ فرستنده
هم نوبت‌ها را بدون تکیه بر رنگ از هم جدا می‌کند، که در تم تیره و برای کم‌بینایی
اهمیت دارد.

### گاردهای امنیتیِ همکار تست‌دار شدند

**چه چیزی عوض شد** شش تستِ تازه ادعاهای امنیتیِ این زیرسیستم را می‌سنجند:
نرسیدنِ کارمندِ بی‌مجوز به دادهٔ دیگران (هم از راهِ ابزارها و هم از راهِ زمینهٔ
پرامپت)، بستنِ سوییچِ فقط-خواندنی در لحظهٔ اجرا، و یکپارچگیِ نقطهٔ تأیید
(مالکیت، اجرای یک‌باره، و لغوِ دسترسی بینِ پیشنهاد و تأیید).

**قبلش چه بود** این گاردها پیاده‌سازی شده بودند و کار می‌کردند، ولی جز چند
موردِ پراکنده تستِ مستقیمی نداشتند.

**چرا اهمیت داشت** این‌ها از آن دسته‌اند که اگر روزی بشکنند، بی‌صدا می‌شکنند:
هیچ خطایی بالا نمی‌آید، فقط داده‌ای دیده می‌شود که نباید.


### خودارزیابی: گاردِ زمانی به‌جای سوییچی که فقط «چه کسی» را می‌گفت

**چه چیزی عوض شد** نمره‌دهنده خودارزیابیِ فرد را تا پایانِ پنجرهٔ نمره‌دهی
نمی‌بیند؛ پس از ثبتِ نمرهٔ خودش می‌بیند.

**قبلش چه بود** دیدنِ خودارزیابی فقط با یک سوییچِ نقش‌محور تعیین می‌شد که
می‌گفت «چه کسی»، نه «چه وقت». هر دو حالتش بد بود: روشن‌کردنش برای مسئول واحد
یعنی او نمرهٔ خودِ فرد را در وضعیت `draft` می‌دید — یعنی دقیقاً سرِ میزِ
نمره‌دهی و پیش از ثبتِ نمرهٔ خودش؛ و خاموش‌گذاشتنش (پیش‌فرض برای هر سه نقشِ
تصمیم‌گیر) یعنی خودارزیابی فقط به منابع انسانی می‌رسید، که نه نمره می‌دهد و نه
در آن گفت‌وگو هست.

**چرا اهمیت داشت** ارزشِ خودارزیابی در کنارِ هم گذاشتنِ دو دیدگاهِ *مستقل*
است. اگر نمره‌دهنده پیش از نمره‌دادن آن را ببیند دیگر دیدگاهِ دوم نیست، لنگر
است؛ و اگر هیچ‌وقت نبیند، آن گفت‌وگو اصلاً اتفاق نمی‌افتد. سوییچ نمی‌توانست بین
این دو یکی را انتخاب کند، چون مسئله زمان بود نه نقش.

### خودارزیابی: پنجره پیوسته شد

**چه چیزی عوض شد** پنجرهٔ ثبتِ خودارزیابی فقط `draft` است.

**قبلش چه بود** `hr_approved` هم در فهرست بود، پس پنجره ناپیوسته می‌شد: باز در
`draft`، بسته در `submitted`، و **دوباره باز** در `hr_approved`.

**چرا اهمیت داشت** آن «دوباره باز» درست بعد از ثبتِ نمرهٔ ارزیاب *و* تأیید
منابع انسانی بود — یعنی همان چیزی که مستنداتِ خودِ همان فایل می‌گفت نباید ممکن
باشد. این عضو بازماندهٔ زمانی بود که مسیر «مدیر» مستقیماً از `hr_approved`
شروع می‌شد؛ آن رفتار برداشته شده بود ولی این‌جا جا مانده بود.

### خودارزیابی: دعوتِ دوم یادآوری است، نه بن‌بست

**چه چیزی عوض شد** منابع انسانی می‌تواند دعوت را دوباره بفرستد.

**قبلش چه بود** بارِ دوم ۴۰۹ می‌گرفت، برای همیشه.

**چرا اهمیت داشت** اگر اعلان گم می‌شد یا کارمند آن را می‌بست، هیچ راهی برای
رساندنِ دوبارهٔ خبر نبود و پنجره — که کوتاه است — بی‌استفاده تمام می‌شد.

### خودارزیابی: اعلانی که به جایی نمی‌رسید

**چه چیزی عوض شد** اعلانِ «خودارزیابی ثبت شد» فقط برای نمره‌دهنده‌ای می‌رود که
سیاستِ محرمانگی اجازهٔ دیدنش را به او داده، و متن می‌گوید چه وقت قابل مشاهده
می‌شود.

**قبلش چه بود** اعلان بی‌قید فرستاده می‌شد، در حالی که پیش‌فرضِ سیاست همان
خودارزیابی را از همان شخص پنهان می‌کرد.

**چرا اهمیت داشت** خبردادن از چیزی که گیرنده هرگز به آن نمی‌رسد، نوفه است و
اعتماد به بقیهٔ اعلان‌ها را کم می‌کند.

### خودارزیابی: تعریفِ پنجره یک جا بیشتر نیست

**چه چیزی عوض شد** پاسخِ «پروندهٔ در جریان» حالا خودش می‌گوید پنجره باز است یا
نه (`self_assessment_open`).

**قبلش چه بود** فرانت فهرستِ وضعیت‌ها را دستی کپی کرده بود.

**چرا اهمیت داشت** آن کپی از بک‌اند جدا افتاده بود و کسی خبر نداشت — همان
دسته خطایی که بی‌صدا می‌ماند تا روزی کاربر چیزی ببیند که نباید.

---

**چه چیزی عوض شد** نام محصول در همه‌جا NexaHR شد: رابط کاربری، مستندات، و
شناسه‌های فنی (نام دیتابیس و کاربرش، نام ایمیج‌ها و والیوم‌های داکر، پیشوند
متریک‌ها، نام کوکی، و متغیرهای محیطی).

**قبلش چه بود** محصول DbsPulse نام داشت و مسیرش با ریپازیتوری V2 مشترک بود.

**چرا اهمیت داشت** از این پس مسیر این محصول از DbsPulse V2 جداست و نام مشترک
باعث قاطی‌شدن دو مسیر می‌شد.

> **برای استقرار — این موارد شکننده‌اند و مهاجرت لازم دارند:**
>
> - نام دیتابیس و کاربرش `dbspulse` → `nexahr` (و در CI: `dbspulse_test` →
>   `nexahr_test`). روی استقرارِ موجود، یا دیتابیس را تغییرِ نام دهید یا
>   `DATABASE_URL` را دست‌نخورده نگه دارید.
> - متغیرهای محیطی `DBSPULSE_ADMIN_USERNAME` و `DBSPULSE_ADMIN_PASSWORD` →
>   `NEXAHR_ADMIN_USERNAME` و `NEXAHR_ADMIN_PASSWORD`. فایل `.env` را به‌روز کنید.
> - پیشوند متریک‌ها `dbspulse_*` → `nexahr_*`. داشبورد و هشدارهای Prometheus
>   باید به‌روز شوند.
> - نام کوکی نشست `dbspulse_refresh` → `nexahr_refresh` — نشست‌های باز یک‌بار
>   باطل می‌شوند و کاربران دوباره وارد می‌شوند.
> - ایمیج‌ها و والیوم‌های داکر `dbspulse-*`/`dbspulse_pgdata` →
>   `nexahr-*`/`nexahr_pgdata`. والیومِ قدیمی خودبه‌خود منتقل نمی‌شود.
> - کلیدهای `localStorage` سمت مرورگر (تم، جمع‌بودن نوار کناری، پیش‌نویسِ
>   خودارزیابی) — تنظیماتِ محلیِ کاربر یک‌بار به حالت پیش‌فرض برمی‌گردد.

**آنچه عمداً تغییر نکرد** نام توسعه‌دهنده (DbsStudio) و لوگوهایش دست‌نخورده
ماندند — آن هویتِ سازنده است، نه نام محصول.

### گزارش‌های ممیزی و نقشهٔ راه از ریپازیتوری حذف شدند

**چه چیزی عوض شد** پوشهٔ `audit/` — یازده گزارش ممیزی، سند رابط کاربری و
درخواست‌های تیم منابع انسانی — و فایل نقشهٔ راه یکپارچه حذف شدند.

**قبلش چه بود** این گزارش‌ها ممیزیِ ریپازیتوری `sanyzrn/DbsPulse_V2` را در کامیت
`ef0166b` توصیف می‌کردند.

**چرا اهمیت داشت** آن‌ها وضعیتِ یک کدبیس دیگر را در یک لحظهٔ گذشته توصیف
می‌کردند و نگه‌داشتنشان در ریپازیتوریِ NexaHR دقیقاً همان قاطی‌شدنی بود که این
نسخه می‌خواست از بین ببرد. متنشان از تاریخچهٔ گیت در دسترس است
(`git show 7a08187:audit/…`).

### نسخهٔ برنامه با واقعیت هم‌تراز شد

**چه چیزی عوض شد** نسخه از `0.9.5` به `1.1.0` رفت (در `package.json`،
`appInfo.ts` و فهرست نسخه‌های داخل برنامه).

**قبلش چه بود** تغییرات v1.0.2 در همین فایل ثبت شده بود ولی نسخهٔ نمایشیِ برنامه
هنوز `0.9.5` بود — یعنی برنامه چیزی را نشان می‌داد که دو نسخه عقب‌تر بود.

**چرا اهمیت داشت** نسخه‌ای که در هدر و فوترِ برنامه دیده می‌شود، تنها چیزی است که
کاربر و پشتیبانی برای گزارش اشکال به آن استناد می‌کنند.

---

## v1.0.2 — شهریور ۱۴۰۵ (سخت‌سازی بر پایهٔ ممیزی)

### یافته‌های ممیزیِ داخلی بسته شد — بدون تغییرِ ظاهر و بدون تغییرِ منطقِ سالم

این نسخه فقط «رفعِ ریسک‌های تأییدشدهٔ ممیزی» است: همان رابط، همان گردش‌کار، همان
قواعد کسب‌وکار — فقط جاهایی که ممیزی نشان داد ناخواسته ناایمن یا نادرست‌اند.
هیچ قابلیت تازه‌ای اضافه نشد و هیچ چیزِ دیگری بازنویسی نشد.

**پرونده‌های مدیران از ساختِ دسته‌ای دیگر بدونِ بررسیِ منابع انسانی نمی‌گذرند**
ساختِ دسته‌ای ارزیابی، پروندهٔ پرسنلِ «مدیر» را مستقیماً در مرحلهٔ بررسیِ معاونت
می‌ساخت — همان اشتباهی که مدت‌ها پیش در مسیرِ تک‌رکوردی اصلاح شده بود ولی به‌دلیل
کپی‌بودنِ منطق، در مسیرِ دسته‌ای باقی مانده بود. نتیجه‌اش می‌توانست پرونده‌ای
نهایی‌شده و چاپ‌شده بدون حتی یک نمره باشد. حالا هر دو مسیر یکسان‌اند: پرونده از
«نمره‌دهی» شروع می‌شود، معاونت نمره می‌دهد، منابع انسانی بررسی می‌کند و مدیرعامل
نهایی می‌کند. و دروازهٔ نهایی‌سازی حالا در خود ماشینِ گردش‌کار قفل شده: هیچ
پرونده‌ای — از رابط یا از دستیار — بدون نتیجهٔ محاسبه‌شده و دست‌کم یک امتیاز
«نهایی‌شده» نمی‌شود.

**دستیار: همهٔ ابزارهای تغییردهنده فقط با تأییدِ انسان**
سه ابزار که بدونِ کارتِ تأیید اجرا می‌شدند (ثبتِ دیدگاه روی پرونده، علامت‌زدنِ
هدفِ برنامهٔ بهبود، اصلاحِ ردیفِ اکسل) حالا مثل بقیهٔ کنش‌های تغییردهنده «در
انتظارِ تأیید» می‌شوند. سوییچِ «اجازهٔ نوشتن» دستیار هم دیگر فقط در فهرستِ
ابزارها سنجیده نمی‌شود؛ در لحظهٔ اجرا هم سنجیده می‌شود، پس هیچ مسیرِ جانبی
نمی‌تواند حالتِ فقط-خواندنی را دور بزند. ثبتِ دیدگاه هم دیگر نسخهٔ موازی ندارد:
از همان مسیرِ رسمیِ رابط می‌گذرد تا مرحله و صندلیِ آن دقیق باشد.

**دستیار: دامنهٔ دیدِ «پرونده‌های روی میز شما» درست شد**
ابزارِ «پرونده‌های بازِ من» برای کارمند عادی، نتیجهٔ نهاییِ *کل سازمان* را
برمی‌گرداند و برای معاونت، صفِ همهٔ معاونت‌ها را — برخلاف رابط که هر دو را
فیلتر می‌کند. حالا هر نقش فقط پرونده‌های صندلیِ خودش را می‌بیند؛ کارمند فقط
کارنامهٔ خودش را.

**دستیار: قاعدهٔ «سازنده ≠ فعال‌کننده» دیگر قابل دورزدن نیست**
قاعدهٔ دومردیِ تغییرِ طرح نمره‌دهی فقط در صفحهٔ رابط سنجیده می‌شد؛ دستیار که
مستقیم سرویس را صدا می‌زد از آن عبور می‌کرد. سنجش به خودِ سرویس منتقل شد تا
*هر* مسیری که فعال‌سازی می‌کند — رابط، دستیار، هر آینده‌ای — از آن عبور نکند.

**دستیار: شکست دیگر نیمه‌کاره جا نمی‌گذارد**
اگر اجرای یک کنشِ تأییدشده وسط کار شکست می‌خورد، نوشته‌های ناقصش با همان
ثبتِ «شکست» ماندگار می‌شدند — مثلاً چند حساب که رمزشان هرگز نشان داده نشده بود.
حالا اول نوشته‌های ناقص دور ریخته می‌شود، بعد وضعیتِ «شکست» ثبت می‌شود؛
همین اصلاح در حلقهٔ گفت‌وگو و در حلقهٔ ابزارها هم اعمال شد و هر پله با کامیتِ
خودش ماندگار می‌شود تا شکستِ یک ابزار، پیشنهادهای سالمِ همان نوبت را هم نبرد.

**دستیار: تأییدِ دوباره‌ی هم‌زمان ممکن نیست**
دو درخواستِ تأییدِ هم‌زمان روی یک کارت می‌توانستند هر دو اجرا را ببینند.
مالکیتِ اجرا حالا با یک به‌روزرسانیِ شرطیِ اتمی گرفته و بلافاصله ماندگار
می‌شود؛ تأییدِ دوم — هر چه هم نزدیک — با «قبلاً تعیین تکلیف شده» رد می‌شود و
کنش دقیقاً یک‌بار اجرا می‌شود.

**زمان‌بند: قفلِ رهبری دیگر نشت نمی‌کند**
قفلِ «فقط یک سرور جارو بزند» به اتصالِ دیتابیس گره خورده بود؛ کامیتِ میانهٔ
جارو اتصال را به استخر برمی‌گرداند و بازکردنِ قفل روی اتصالِ دیگری بی‌صدا
شکست می‌خورد — تا بازیافتِ اتصال، همهٔ سرورها فکر می‌کردند رهبر دیگری هست و
یادآوری‌ها می‌ایستاد. حالا قفل روی اتصالِ اختصاصی نگه داشته و حتماً روی همان
اتصال آزاد می‌شود.

**گزارش‌ها: خطای سرور دیگر «بدون داده» نمی‌خواند**
اگر سرویسِ گزارش‌ها خطا می‌داد، داشبورد با تمامِ اطمینانِ بصری «صفر ارزیابی»
و «داده‌ای وجود ندارد» نشان می‌داد — نتیجه‌گیریِ غلطی که منابع انسانی از آن
می‌ساخت. حالا خطا با پیامِ روشن و دکمهٔ «تلاش دوباره» از خالیِ واقعی تفکیک
می‌شود.

**و چند سخت‌سازیِ کوچک‌تر**
بازکردنِ پروندهٔ ارزیابی روی زنجیره‌ای که یکی از صندلی‌هایش (مسئول واحد،
معاونت یا مدیرعامل) غیرفعال شده ممنوع شد — در مسیرِ تک‌رکوردی با پیامِ روشن و
در پیش‌نمایشِ ساختِ دسته‌ای با وضعیتِ «بلاک با دلیل». لاگِ ممیزیِ دستیار حالا
آرگومان‌ها را به‌صورت بازگشتی پاک‌سازی می‌کند تا رمز یا گذرواژه — حتی داخلِ
آرگومان‌های تودرتو مثل اصلاحِ سلولِ «رمز اولیه» اکسل — هرگز در لاگ ننشیند.
و سقفِ حجمِ آپلود در پیکربندیِ nginx با سقفی که بک‌اند واقعاً قبول می‌کند
(۲۰ مگابایت) یکی شد؛ پیش‌تر فایل‌های بزرگ‌تر از ۱ مگابایت پشتِ پراکسی با
خطای مبهم رد می‌شدند.

**آزمون‌ها** برای هر مورد بالا آزمونِ رگرسیون اضافه شد — از جمله یک مسابقهٔ
واقعیِ دو-اتصاله برای تأییدِ هم‌زمان و یک مسابقهٔ نشتِ قفلِ زمان‌بند. سوئیت
بک‌اند ۷۴۲/۷۴۲ و فرانت‌اند ۱۷۲/۱۷۲ سبز است؛ ساختِ تولیدی و اعتبارسنجیِ
مایگریشن روی دیتابیسِ تازه هم بی‌خطا.

> یادداشتِ استقرار: اگر پیش از این نسخه از ساختِ دسته‌ای استفاده شده باشد،
> پرونده‌های مدیریِ ساخته‌شده در وضعیتِ «بررسیِ منابع انسانی» ممکن است در
> دیتابیس مانده باشند؛ آن‌ها را با لغوِ پرونده و ساختِ دوباره تکلیف کنید —
> این نسخه عمداً هیچ رکوردِ منابع انسانی را بی‌صدا بازنویسی نمی‌کند.

## v1.0.1 — شهریور ۱۴۰۵ (صیقلِ ظاهری)

### همین سامانه، جمع‌وتر و روان‌تر

**چه عوض شد** کل رابط یک پله جمع شد و حرکت‌هایش سریع: پدینگ کارت‌ها از `p-5` به `p-4`،
جدول‌ها یک پله کوتاه‌تر، دکمه‌ها و تب‌ها باریک‌تر، عنوانِ صفحه از درشت‌نماییِ دو پله به
یک پله درست، نوارهای پیشرفت نازک‌تر، و نمودارها کوتاه‌تر (۲۸۰/۲۶۰ پیکسل به‌جای ۳۴۰/۳۰۰)
با تولتیپِ سبک‌تر و انیمیشنِ روندِ نصف‌شده. زمانِ همهٔ انتقال‌های رابط — تب، مودال،
کشو، تعویض صفحه، شمارنده‌ها — به‌طور یکدست کوتاه شد (۰٫۲۲ تا ۰٫۳ ثانیه به‌جای ۰٫۵ تا
۰٫۷) و دکمه‌ها در لحظهٔ کلیک یک فشردگیِ خیلی ریز می‌گیرند. مودال از گوشه‌های خیلی گرد
به گردیِ استاندارد بقیهٔ سامانه رسید، اسکرول‌بار خنثی شد، انتخابِ متن ته‌رنگِ برند گرفت،
و صفحهٔ «کارنامه من» وقتی هیچ بخشی برای حساب فعال نیست، دیگر سفیدِ خالی نیست — جمله‌ای
آرام می‌گوید چرا خالی است.

**قبلش چه بود** کارت‌ها و جدول‌ها فضای نفسِ بیشتری می‌خوردند و در داشبوردها یعنی اسکرولِ
بیشتر برای همان داده؛ حرکت‌ها عمداً سنگین و کند شده بودند («حس لطافت») ولی در کارِ
روزمره هر تعویض تب یک مکثِ محسوس بود؛ و هر نمودار حدود ۸۰ پیکسل بلندتر از چیزی بود که
داده‌اش می‌خواست.

**چرا اهمیت داشت** این‌ها صفحاتی هستند که منابع انسانی همه‌روز باز می‌کند؛ تراکمِ
اطلاعاتِ بالاتر و پاسخِ سریع‌تر یعنی کارِ بیشتر در همان نگاه. و چون همهٔ این‌ها روی
اجزای مشترک (کارت، جدول، دکمه، مودال، توکن‌های حرکت) انجام شد، همهٔ صفحات با هم عوض
شدند نه تکه‌تکه — هویتِ بصری همان قرمزِ برند و کارتِ مرزدارِ قبلی است، فقط جمع‌وتر و
سریع‌تر. هیچ گردش‌کاری، مجوزی یا منطقی دست نخورده؛ همهٔ ۱۷۰ آزمونِ فرانت‌اند و ساختِ
تولیدی سرِ جایشان سبزند.

## v1.0.0 — شهریور ۱۴۰۵ (بازسازی دستیار)

### همکار NexaHR — از «چت‌بات» به «همکارِ همه‌کاره»

**دستیار حالا کار می‌کند، نه فقط حرف می‌زند**
نسخهٔ قبل دستیار، متنِ خلاصه‌ای از داده‌ها را یک‌بار به مدل می‌داد و شش کنشِ
ثابت داشت. حالا دستیار ۴۷ ابزارِ واقعی دارد — پرسنل، حساب‌ها، واحدها، زنجیرهٔ
ارزیابی، مجوزها، پرونده‌ها، گذارهای گردش‌کار، شاخص‌ها، طرح نمره‌دهی، دوره‌ها،
آغاز گروهی، برنامه‌های بهبود، گزارش‌های تحلیلی و گزارش رویدادها — و در یک نوبت
می‌تواند چند پله کار کند: جست‌وجو کند، جزئیات بخواند، و بعد پیشنهاد بدهد.

**همان اختیاراتِ خودتان، در گفت‌وگو**
هر ابزار همان دروازه‌ای را دارد که صفحهٔ متناظرش دارد؛ گارد در لحظهٔ *اجرا*
سنجیده می‌شود، نه فقط در فهرستی که به مدل نشان داده می‌شود. مسئول واحد از
دستیار هم فقط پرونده‌های خودش را می‌بیند؛ پشتیبانی فنی به هیچ پرونده‌ای دست
نمی‌رسد.

**هیچ تغییری بدون یک کارتِ تأیید**
کنش‌های تغییردهنده اجرا نمی‌شوند؛ «در انتظارِ تأیید» می‌شوند و کارتشان در
گفت‌وگو می‌ماند — با جزئیات کاملِ پیشنهاد. تأیید فقط از همان نقطه اجرا می‌شود
و همان‌جا برای بار دوم: مالکیتِ پیشنهاد، زنده‌بودنش، مجوزِ *امروزِ* کاربر و
سالم‌بودنِ آرگومان‌ها. هر پیشنهاد یک‌بارمصرف است؛ منقضی هم می‌شود.

**اکسلِ ناقص دیگر «خطای مبهم» نیست**
فایل را در گفت‌وگو می‌اندازید؛ دستیار آن را با همان اعتبارسنجیِ رسمیِ ورودِ
دستی می‌خواند، خطای هر ردیف را ردیف‌به‌ردیف توضیح می‌دهد و مقدارِ جاافتاده را
*می‌پرسد*. جوابِ شما (مثلاً «۱۴۰۶/۰۶/۰۱») روی خودِ فایل می‌نشیند، اعتبارسنجی از
نو اجرا می‌شود، و ورودِ نهایی — پرسنل، زنجیره و حساب‌ها — فقط با تأییدِ شما و
از همان مسیری است که فرمِ دستی می‌رود. رمزهای موقت هم فقط یک‌بار نمایش داده
می‌شوند.

**هر قدمش ممیزی‌پذیر است**
فراخوانیِ هر ابزار، نتیجه و شکستش، تأیید و ردِ هر پیشنهاد — همه در گزارش
رویدادها با زنجیرهٔ هش ثبت می‌شوند.

**گفت‌وگوها ماندگار شدند**
تاریخچهٔ گفت‌وگوها بین باز و بسته‌کردن‌ها می‌ماند؛ گام‌هایی که دستیار برد و
کارت‌های در انتظارِ تصمیم، با وضعیتِ زنده‌شان بازسازی می‌شوند. پنجرهٔ شناور
همچنان هست و با یک کلیک به صفحهٔ کامل (`/copilot`) باز می‌شود.

**تنظیماتِ تازه برای مدیر**
عمقِ کاریِ دستیار در هر نوبت، اجازه و سقفِ بارگذاری فایل. سازگاری با
سرویس‌هایی که فراخوانیِ بومیِ ابزار ندارند خودکار است — با پروتکل جایگزین
کار می‌کنند، بی‌آنکه قابلیتی از دست برود.

---

## v0.9.0 — شهریور ۱۴۰۵

### دستیار هوشمند

**گفت‌وگو به زبان خودتان، دربارهٔ دادهٔ همین سامانه**
دستیار به پرسش‌هایی مثل «چند نفر قراردادشان تا آخر سال تمام می‌شود؟» یا «الهام
رستمی در کدام واحد است؟» جواب می‌دهد، و برای کارهایی مثل ثبت پرسنل یا فرستادن
دعوت خودارزیابی *پیشنهاد* می‌دهد.

**مدل پیشنهاد می‌دهد، کاربر تصمیم می‌گیرد**
هیچ تغییری خودبه‌خود اجرا نمی‌شود. وقتی پاسخ به یک کنش تبدیل می‌شود، به‌جای
JSON یک *جمله* به زبان کاربر نوشته می‌شود و زیرش یک دکمه؛ تا آن دکمه فشرده
نشود هیچ‌چیز عوض نمی‌شود.
*چرا قابل مذاکره نیست:* مدلی که «کدام نمره را برای احمدی ثبت کردم؟» را «پروندهٔ
احمدی را حذف کن» بفهمد، روی سرویس‌های ارزان یک فرض دور از ذهن نیست، و
عذرخواهیِ بعدش ردیفِ حذف‌شده را برنمی‌گرداند.

**دسترسیِ دستیار از دسترسیِ خودِ کاربر بیشتر نیست**
مسئول واحد در متنِ فرستاده‌شده به مدل فقط زیرمجموعهٔ خودش را می‌بیند، دقیقاً مثل
صفحه‌ای که باز می‌کند؛ و کنشی که از راه رابط اجازه‌اش را ندارد، از این‌جا هم رد
می‌شود. گارد در *مجری* است نه در متنِ راهنما — متن راهنما یک پیشنهاد است.

**تعیین اینکه چه کسی دستیار دارد**
مدیر سامانه برای هر حساب جداگانه روشنش می‌کند، و می‌تواند برای هرکدام کلید API،
مدل و سقف پیام روزانهٔ جدا بگذارد تا هزینه و سهمیه از هم جدا بماند. کاربرِ
دستیار — مثلاً معاونت — فقط پنجرهٔ گفت‌وگو را می‌بیند، نه تنظیمات را.
*چرا:* جدا‌سازی در سطح *مجوز* است (`manage_ai`) و نه پنهان‌کردن یک دکمه در
فرانت‌اند، که هیچ چیزی را محافظت نمی‌کند.

**کلیدهای API رمزنگاری‌شده ذخیره می‌شوند**
قاعدهٔ سامانه این بوده که راز به دیتابیس نرود، چون بک‌آپ دیتابیس جاهایی می‌رود
که `.env` نمی‌رود. کلیدِ هر کاربر این قاعده را نمی‌پذیرد — به یک *ردیف* گره
خورده. پس رمز می‌شود، با کلیدی که در `.env` می‌ماند: بک‌آپِ لو رفته به تنهایی
هیچ کلید معتبری نمی‌دهد. API هم هرگز مقدار را برنمی‌گرداند، فقط چهار نویسهٔ آخر.

**سرویس‌های آماده**
Anthropic، OpenAI، Google Gemini، OpenRouter و «سفارشی». یک کلیک، آدرس سرویس و
یک مدلِ پیش‌فرضِ سالم را با هم می‌گذارد.
*چرا:* تا امروز مدیر باید آدرس را از حفظ می‌نوشت، و نیمی از مشکلات راه‌اندازی
همان یک `/v1` جامانده بود — با پیام خطایی که دربارهٔ آدرس چیزی نمی‌گفت. هر پنج
سرویس از یک آداپتور رد می‌شوند، چون Anthropic و Google هرکدام یک نقطهٔ پایانیِ
سازگار با OpenAI منتشر می‌کنند. مدلِ پیش‌فرض یک *پیشنهاد* است و قابل ویرایش
می‌ماند: نام مدل‌ها سریع عوض می‌شوند و قفل‌کردنشان در کد یعنی هر تغییرِ آن‌ها یک
استقرارِ تازه.

**تنظیماتِ نحوهٔ پاسخگویی**
متن دستورالعمل، «فقط دربارهٔ همین سامانه پاسخ بده یا نه»، مقدار دادهٔ ارسالی در
هر پرسش، خلاقیت، طول پاسخ، مهلت و سقف طول پیام — همه از پنل مدیریت.
*چرا مقدار داده یک تنظیم است و نه جزئیات پیاده‌سازی:* هرچه در آن متن برود از
سازمان بیرون رفته، و این تصمیمِ مدیر است نه تصمیمِ کد.

**خطاها جملهٔ خودِ سرویس را نشان می‌دهند**
تفاوت ۴۰۱ با «مدل پیدا نشد» دو رفعِ متفاوت است و کاربر روی هر دو می‌تواند کاری
بکند؛ «مشکلی پیش آمد» هیچ‌کدام را نمی‌گوید. دکمهٔ «آزمودن اتصال» هم یک درخواست
واقعی می‌فرستد و همان جمله را برمی‌گرداند.

---

## v0.8.0 — شهریور ۱۴۰۵

نخستین نسخه‌ای که برای بهره‌برداری واقعی آماده است. تغییرِ کدِ اجرایی کم است؛
آنچه اضافه شد، مسیرِ رسیدنِ همین کد به یک سرور واقعی است بدون اینکه چیزی که
نباید، همراهش برود.

### استقرار

**اجرا بدون تحویل کد منبع**
`docker-compose.prod.yml` به‌جای `build` از `image` استفاده می‌کند، یعنی روی
ماشین مقصد فقط همان فایل و `.env` لازم است — نه پوشهٔ `backend/`، نه
`frontend/`، نه `.git`. ایمیج‌ها روی سیستم توسعه ساخته و با `docker save`
منتقل می‌شوند.
*چرا:* فرانت‌اند در ایمیج فقط جاوااسکریپتِ ساخته‌شده است و اصلاً سورس ندارد؛
بک‌اند فایل‌های پایتون را دارد ولی داخل ایمیج، نه روی دیسکِ سرور. راهنما
(`deploy/PRODUCTION.md`) صادقانه می‌گوید این چه چیزی را محافظت می‌کند و چه
چیزی را نه — روی ماشینی که در اختیار طرف مقابل است، محافظتِ مطلق ممکن نیست.

**نام صریح برای ایمیج‌ها**
تا امروز نام ایمیج از روی نام پوشهٔ پروژه ساخته می‌شد، یعنی `docker save` روی
دو ماشین دو نام مختلف می‌دید. حالا `nexahr-backend` و `nexahr-frontend`.

**هشدار روی اسکریپت راه‌اندازی محلی**
`setup_and_run.bat` عمداً `ENVIRONMENT=development`، یک `JWT_SECRET_KEY` ثابت،
کاربران نمونه با رمز مشترک، و `MIN_COHORT_SIZE=1` می‌نویسد — که سرکوبِ آمارِ
گروه‌های کوچک را خاموش می‌کند. حالا این را در همان سرِ فایل می‌گوید و به راهنمای
استقرار ارجاع می‌دهد.
*چرا:* اسکریپتی که روی لپ‌تاپ درست کار می‌کند، روی سرور سازمان یک استقرارِ
ناامن می‌سازد که هیچ نشانهٔ بیرونی‌ای از خرابی ندارد.

### دربارهٔ پلتفرم

بخش «سازنده» به صفحهٔ عمومی اضافه شد.

---

## v0.7.1 — شهریور ۱۴۰۵

### رابط

**پوستهٔ شناور و دورگرد**
ناوبری، نوار بالا و پاصفحه هر سه به لبهٔ پنجره چسبیده بودند و گوشه‌هایشان تیز
بود. حالا هر سه یک اندازه از لبه فاصله دارند و گرد شده‌اند — در حالت جمع‌شدهٔ
منو هم همان قاب، نه یک ستونِ چهارگوشِ چسبیده.
*چرا:* نوار بالا شناور است ولی محتوایی که زیرش رد می‌شود نباید از فاصلهٔ بالای
آن پیدا باشد؛ یک لایهٔ هم‌رنگِ صفحه پشتِ نوار تا لبه کشیده می‌شود و همین را حل
می‌کند.

**نام هر بخش روی نشانه‌های منوی جمع‌شده**
`title` بومی مرورگر با حدود یک ثانیه تأخیر و در گوشهٔ اشاره‌گر ظاهر می‌شد، یعنی
کاربر باید روی هر نشانه مکث می‌کرد تا بفهمد کدام است. حالا برچسب بی‌درنگ کنارِ
خودِ نشانه می‌آید.

**افکت ورودِ تب شاخص‌ها**
انیمیشن یک لایه پایین‌تر از بقیهٔ تب‌ها بود: قابِ کارت و سرستون‌هایش بی‌حرکت
می‌ماندند و فقط ردیف‌های داخل محو و ظاهر می‌شدند. حالا کلِ کارت می‌آید، مثل هر
تب دیگر.

**چشمِ نمایش رمز، همه‌جا**
صفحهٔ ورود چشمِ اختصاصی خودش را داشت — با برچسب انگلیسی و در سمتِ مخالفِ بقیهٔ
فرم‌ها — و رمز موقتِ فرم ثبت پرسنل اصلاً پنهان نمی‌شد. حالا هر دو همان ورودیِ
مشترک را می‌گیرند.
*چرا:* قاعده‌های رمز (طول، تولید، سنجهٔ قدرت) دو نسخه داشتند و یک رمز می‌توانست
در یک فرم «قوی» و در فرم دیگر «متوسط» باشد. نسخهٔ دوم حذف شد.

### منابع انسانی

**دعوت به خودارزیابی**
خودارزیابی از قبل کار می‌کرد ولی هیچ‌کس خبر نداشت: کارمند فقط اگر خودش وارد
سامانه می‌شد و پروندهٔ بازش را پیدا می‌کرد می‌فهمید که می‌تواند نظرش را ثبت کند.
حالا منابع انسانی از فهرست پرسنل دکمه را می‌زند، کارمند اعلان می‌گیرد، و وضعیتِ
هر نفر در همان ردیف دیده می‌شود.
*چرا مسدودکننده نشد:* «تا کارمند ثبت نکند، مسئول واحد نتواند شروع کند» یعنی یک
کارمندِ در مرخصی کلِ چرخهٔ سازمان را متوقف می‌کند. به‌جایش پنجرهٔ خودارزیابی
*پیش از* قطعی‌شدن نمرهٔ ارزیاب باز است و مسئول واحد پیش از ثبت می‌بیند که ثبت
شده یا نه.

**روند میانگین سازمان**
بقیهٔ داشبورد «الان چطوریم» را می‌گوید. این تنها جایی است که می‌گوید «داریم بهتر
می‌شویم یا بدتر»: میانگین امتیاز نهایی در هر دورهٔ ارزیابی، با همان فیلتر محل.
*چرا:* با کمتر از دو دوره نموداری کشیده نمی‌شود و علتش نوشته می‌شود؛ و دوره‌ای
که میانگینش به‌خاطر کم‌بودن جمعیت پنهان شده *شمرده* می‌شود، چون خطی که از رویش
بپرد شکافِ داده را شبیهِ روندِ صاف نشان می‌دهد.

### مدیر سامانه

**واحدهای سازمانی، تعریف‌شده و نه استخراج‌شده**
تا امروز فهرست واحدها از خودِ داده درمی‌آمد: هر رشته‌ای که در پروندهٔ کسی نوشته
شده بود یک واحد بود. یعنی یک غلط تایپی بی‌سروصدا واحد تازه می‌ساخت، و واحدی که
هنوز کسی در آن نبود اصلاً وجود نداشت.
*چرا:* تغییر نام یک واحد، پرسنلِ همان واحد را هم با خودش می‌برد — وگرنه سامانه
دو واحد نشان می‌داد، یکی در فهرست و یکی در داده. حذف فقط برای واحدِ خالی است؛
واحدی که سابقه دارد «غیرفعال» می‌شود تا گزارش‌های گذشته دست‌نخورده بمانند.

**قاعده‌های سازمانی از پنل، نه از فایل تنظیمات سرور**
«مهلت اعتراض هفت روز است یا ده روز» و «از چند نفر به بالا میانگین را نشان بده»
تصمیم‌های سازمان‌اند، ولی عوض‌کردنشان به دسترسی SSH نیاز داشت. هشت عدد به پنل
آمدند: مهلت اعتراض، حداقل جمعیت برای نمایش میانگین، هشدار پایان قرارداد،
یادآوری پروندهٔ راکد، یادآوری بازبینی برنامهٔ بهبود، فاصلهٔ تکرار اعلان، و دو
عددِ قفل‌شدن حساب.
*چرا:* کف و سقفِ هر عدد در سرور اعمال می‌شود و نه فقط در فرم — یک «حداقل جمعیت
= ۰» که از راه API بنشیند، ناشناس‌ماندن را بی‌سروصدا خاموش می‌کند. اندازهٔ استخر
اتصال و مهلت توکن عمداً نیامدند: آن‌ها تصمیم استقرارند.

**ویرایش دورهٔ ارزیابی**
نام و تاریخ‌های یک دوره قابل اصلاح شدند. وضعیتِ دوره عمداً از این فرم بیرون
ماند: بستنِ دوره قواعد خودش را دارد.

---

## v0.7.0 — شهریور ۱۴۰۵

### مدیر سامانه

**دسترسیِ کامل به راه‌اندازی**
ایراد ساده بود — «دسترسی‌های ادمین کم است» — ولی ریشه‌اش جای دیگری بود: چند کارِ
راه‌اندازی به *نقش* `hr` بسته بودند نه به مجوز، و حساب مدیر سامانه عمداً نقشی در
زنجیرهٔ ارزیابی ندارد. یعنی مجوزِ ساختِ حساب را داشت، ولی نه راهی به صفحهٔ
«کاربران» و نه امکان ثبتِ پرسنلی که آن حساب‌ها را به او وصل کند.
*چرا:* مدیر سامانه باید بتواند سازمان را از صفر راه بیندازد — معاونت و مدیرعامل و
مسئول واحد بسازد، پرسنل ثبت کند، و زنجیرهٔ هرکس را تعیین کند. مرزی که برداشته
نشد: همچنان نمرهٔ هیچ‌کس را نمی‌بیند.

**حساب مدیر همیشه وجود دارد**
تا امروز ساختِ اولین حساب یک اسکریپت دستی بود. نصبِ تازه بدون آن، سرویسی بود که
بالا می‌آمد و هیچ‌کس نمی‌توانست واردش شود؛ و اگر تنها حساب مدیر غیرفعال می‌شد،
تنها راه برگشت SQL دستی روی دیتابیس بود. حالا در هر بالا آمدنِ سرویس، اگر هیچ
حساب *فعالی* با مجوز اختیاردهی نباشد، یکی ساخته می‌شود.
*چرا:* رمز از متغیر محیطی می‌آید یا تصادفی ساخته و یک‌بار در لاگ نوشته می‌شود —
هرگز رمزِ ثابت، و همیشه با اجبارِ تغییر در اولین ورود.

**حذف حساب کاربری**
تا امروز فقط «غیرفعال کردن» بود. حالا حذف کامل هم هست، با یک مرز: حسابی که در
گزارش رویدادها رد دارد حذف نمی‌شود.
*چرا:* آن لاگ یک زنجیرهٔ هش است و پاک‌کردن یک ردیفش بقیه را غیرقابل‌اثبات می‌کند.
پیام خطا همین را می‌گوید و «غیرفعال کردن» را پیشنهاد می‌دهد — نه یک خطای مبهم.

### رابط

**ناوبری به ستون کناری رفت**
ناوبری افقی از عرضِ هر صفحه می‌دزدید و منابع انسانی ده آیتم دارد. در ستون، فهرست
عمودی است و آیتم بعدی به هیچ‌چیز فشار نمی‌آورد. زیر lg همان ستون یک کشو می‌شود.

**تعیین رمز**
چشمِ نمایش، سنجهٔ قدرت، ساختِ رمز قوی و کپی — همه در یک جا.
*چرا:* تعیین رمز برای کسِ دیگر سه کارِ پشت سر هم است و هر سه دستی بودند. نتیجه‌اش
رمزهایی بود که چون باید تایپ و گفته می‌شدند، ساده انتخاب می‌شدند.

### داده و گزارش

**سه محل: دفتر مرکزی، کارخانه، مدرپ‌ها**
فهرست محل‌ها *استخراج‌شده* بود — هرچه در واحدها پیدا می‌شد. یعنی محلی که هنوز
هیچ‌کس در آن ثبت نشده بود در هیچ فیلتری وجود نداشت، و برای ثبتِ اولین نفر در
«مدرپ‌ها» باید اول کسی در «مدرپ‌ها» می‌بود. حالا سه محل همیشه هستند، در فرم
پرسنل از یک فهرست بسته انتخاب می‌شوند، و ایمپورت و خروجی اکسل هم ستون «محل»
دارند.

**«وضعیت پرونده‌های ارزیابی» جای «قیف گردش‌کار»**
قیف یک عدد در هر مرحله می‌داد: می‌گفت کجا شلوغ است، ولی نه چرا. صفِ ده‌تایی که هر
پرونده‌اش نیم روز می‌ماند سالم است و صفِ دوتایی که هر کدام دو هفته مانده‌اند نیست.
حالا هر مرحله می‌گوید چند پرونده در جریان، از چند، چند درصد، روی میزِ چه کسی، چند
بسته شده، میانگین توقف، قدیمی‌ترین پروندهٔ در جریان، و چند بار کار به این مرحله
*برگشته*. با کلیک، همان اعداد به‌ازای هر شخص باز می‌شوند.

**چهار کارت بالای داشبورد**
کل پرسنل مشمول ارزیابی، در انتظار بررسی منابع انسانی، نهایی‌شده، درصد تکمیل.

**درصد افراد مطلوب و نیازمند بهبود**
میانگین به‌تنهایی توزیع را پنهان می‌کند: سازمانی که نصفش عالی و نصفش ضعیف است،
همان میانگینِ سازمانی را دارد که همه‌اش متوسط‌اند. مرزهای این دو درصد از خودِ
«طرح نمره‌دهی» می‌آیند، نه از عددی که در کد نوشته شده باشد.

**مقایسهٔ واحدها با تفکیک امتیاز**
هر واحد حالا امتیاز کل، عمومی (رفتاری) و تخصصی را جدا نشان می‌دهد.
*چرا:* واحدی که نمرهٔ کلش خوب است ممکن است در یکی از دو بخش ضعیف باشد و در دیگری
قوی — و عددِ کل دقیقاً همان را پنهان می‌کند.

**ضعیف‌ترین و قوی‌ترین شایستگی‌ها**
دو تب، جدا برای شاخص‌های عمومی و تخصصی.
*چرا:* فهرستی که فقط ضعف نشان می‌دهد هر سازمانی را بیمار جلوه می‌دهد و هیچ‌وقت
نمی‌گوید کجا باید همان کار را تکرار کرد.

**«افراد نیازمند توجه»**
نام قبلی «کمترین امتیاز به تفکیک فرد» بود — توصیفِ کوئری، نه کاری که باید انجام
شود. حالا با فیلتر محل هم می‌آید.

### دو باگ

**«ادامه ارزیابی باز» پس از ثبت**
پس از ثبت یک ارزیابی، همان دکمه تا رفرش صفحه سر جایش می‌ماند. دلیلش یک مسابقه
بود: باطل‌کردن کش منتظر نمی‌ماند و ناوبری زودتر می‌رسید، و صفحهٔ بعدی — که در
لحظهٔ باطل‌سازی هنوز mount نشده بود — فهرست را از کش می‌گرفت.
*چرا اهمیت داشت:* ارزیاب فکر می‌کرد ثبت انجام نشده و دوباره کلیک می‌کرد.

**شمارهٔ ردیف در فرم امتیازدهی**
کامنت‌های زنجیره به شاخص‌ها ارجاع می‌دهند و تا امروز تنها راهش نوشتنِ عنوان کامل
بود. شماره در کل فرم پیوسته است — نه در هر بخش جدا — چون دو ردیفِ «۳» در یک
پرونده همان ابهامی را می‌سازد که این ستون برای رفعش آمده.

### تأییدشده، بدون تغییر

زنجیره‌ای که معاونت ندارد، پس از تأیید منابع انسانی مستقیم به مدیرعامل می‌رود.
این از نسخهٔ ۰٫۴ درست بوده و تست دارد؛ دوباره بررسی و تأیید شد.

---

## v0.6.0 — شهریور ۱۴۰۵

بازطراحی رابط. هیچ قابلیتی حذف یا اضافه نشد و هیچ قاعدهٔ کاری تغییر نکرد؛ چیزی
که عوض شد این است که رابط چطور آنچه را از قبل داشت نشان می‌دهد. دو باگ هم در
همین مسیر پیدا و درست شدند.

### پوستهٔ برنامه

**نوار بالای تمام‌عرض به‌جای کارت شناور**
هدر قبلی یک کارتِ شناورِ دوردیفی بود (برند بالا، ناوبری پایین) روی زمینه‌ای که
یک شبکهٔ متحرک هم پشتش می‌چرخید، و محتوا در ستونی وسط‌چین با عرض محدود می‌نشست.
حالا یک نوارِ چسبیده به بالا با یک خط مرزی است و ناوبری در همان ردیف جا می‌شود.
*چرا:* روی صفحه‌های داده‌محور، ارتفاعِ هدر و حاشیهٔ دو طرف مستقیماً از جدول کم
می‌شود. زیر `lg` ناوبری به یک کشو می‌رود، چون ده آیتم در عرض کم یا می‌شکند یا
اسکرول افقی می‌سازد.

### خواندنِ داده

**جدول‌ها روی موبایل کارت می‌شوند**
یک جدول چهارستونی در عرض ۳۹۰ پیکسل هر کلمه را به سه سطر می‌شکست و ستون آخر
بی‌آنکه معلوم باشد از لبه بیرون می‌زد.
*چرا:* بخشی از داده روی موبایل اصلاً دیده نمی‌شد. هر ردیف حالا یک کارت است با
برچسب کنار هر مقدار. فقط یکی از دو نما ساخته می‌شود، نه هر دو با پنهان‌کردنِ
CSS — وگرنه خوانندهٔ صفحه هر ردیف را دو بار می‌خواند.

**قیف گردش‌کار واقعاً قیف شد**
پنج کاشیِ هم‌اندازه بود، یعنی دقیقاً چیزی را پنهان می‌کرد که قرار بود نشان بدهد:
کجا پرونده تلنبار شده. حالا پنج نوارِ افقی با طولِ متناسب با تعداد.

**تکرار در داشبورد منابع انسانی حذف شد**
«کل ارزیابی‌های نهایی‌شده» عیناً همان عددی بود که نوار بالای صفحه نشان می‌داد.
سه کارتِ ردیف دوم به یک جمله تبدیل شدند.

**وضعیت پرونده در همان ردیفِ فرد**
مسئول واحد برای فهمیدن اینکه پروندهٔ یک نفر کجاست، باید اسم‌ها را بین دو جدولِ
صفحه تطبیق می‌داد.

**نمودار ترکیب نتیجه**
هر چهار میله یک قرمز بودند — «عدم تمدید» و «تمدید با شرایط استاندارد» دقیقاً یک
شکل دیده می‌شدند. حالا از بدترین بند به بهترین مرتب‌اند و رنگشان از جایشان در
نردبانِ طرحِ فعال می‌آید، نه از متنِ برچسبشان (که قابل تغییر است).

**نام ارزیاب به‌جای نام کاربری**
جدول «نمرات هر ارزیاب» فقط `sup_it` را نشان می‌داد.

### رنگ

**قرمز فقط برای کنش**
قرمزِ برند روی نام افراد، لینک‌های ردیف، نشانِ وضعیت «تأییدشده توسط HR»، دکمهٔ
«غیرفعال کردن» و مرزِ فوکوسِ ورودی‌ها هم نشسته بود.
*چرا:* رنگی که همه‌جا باشد هیچ‌جا معنا ندارد. بدترین موردش مرزِ فوکوسِ ورودی‌ها
بود: مرزِ قرمز روی یک ورودی در هر رابطی یعنی «این را اشتباه پر کرده‌ای» — و همین
سامانه برای ورودیِ واقعاً نامعتبر هم قرمز می‌گذاشت. وضعیت «تأییدشده توسط HR» هم
نیلی شد تا زنجیرهٔ وضعیت‌ها خاکستری ← آبی ← نیلی ← کهربایی ← سبز خوانده شود.

**فقط استثنا علامت می‌خورد**
در فهرست شاخص‌ها، بیست نشانِ سبزِ «فعال» پشت سر هم هیچ چیزی نمی‌گفتند. حالا فقط
شاخصِ *غیرفعال* نشان می‌گیرد.

### فرم نمره‌دهی

**عرض ستون‌ها متعادل شد**
ستون «مصداق رفتاری» که فقط خوانده می‌شود دو سومِ عرض را می‌گرفت و دو ستونی که
ارزیاب واقعاً با آن‌ها کار می‌کند — امتیاز و شواهد — فشرده بودند.

**نوار «ذخیره / ثبت» به کف صفحه چسبید**
فرم بیست شاخص دارد و هم دکمه‌ها و هم پیامِ «هنوز فلان‌قدر شاخص امتیاز ندارد»
انتهای همان اسکرول بودند؛ ارزیاب فقط وقتی می‌فهمید چیزی جا مانده که به ته صفحه
می‌رسید.

### دو باگ

**«ادامه ارزیابی باز» روی پرونده‌ای که دست مسئول واحد نیست**
رابط همه‌جا «هر چیزی جز نهایی‌شده» را «باز» می‌شمرد. یعنی پروندهٔ **لغوشده** هم
جلوی شروع ارزیابی تازه را می‌گرفت، و پروندهٔ ثبت‌شده‌ای که رفته بود روی میز
منابع انسانی، دکمهٔ «ادامه ارزیابی» می‌گرفت و کاربر را به صفحه‌ای می‌برد که هیچ
کاری در آن نمی‌توانست بکند. دکمه حالا سه حالت دارد: ادامهٔ پیش‌نویس، مشاهدهٔ
پروندهٔ در جریان، شروع ارزیابی جدید.

**مدت‌زمانِ منفی**
در «سرعت چرخهٔ تصمیم»، مدت‌زمانِ کمتر از یک روز به‌صورت عددِ اعشاری نشان داده
می‌شد و در حالت‌های مرزی می‌توانست منفی از آب دربیاید — که برای یک مدت‌زمان
بی‌معناست. حالا «کمتر از یک روز» نوشته می‌شود.

---

## v0.5.0 — شهریور ۱۴۰۵

این نسخه تقریباً یکسره از یک ممیزی بیرونیِ فرایند HR درآمد. هر ادعای آن گزارش
در کد بررسی شد — بخشی تأیید شد، بخشی رد (مهم‌ترینش این ادعا که کارمند شواهدِ
نمره‌اش را نمی‌بیند، در حالی که کارنامهٔ PDF خودش همه را دارد). این‌ها موارد
تأییدشده‌اند.

### تفکیک وظایف و درستیِ فرایند

**مسیر «مدیر» هم مرحلهٔ بررسی منابع انسانی گرفت**
پروندهٔ پرسنلِ علامت‌خورده به‌عنوان مدیر مستقیماً در وضعیت «تأییدشده توسط HR»
ساخته می‌شد: معاونت نمره می‌داد و **خودش همان نمره را تأیید می‌کرد** و پرونده
می‌رفت روی میز مدیرعامل. مرحلهٔ بررسی منابع انسانی در این مسیر هرگز رخ نمی‌داد.
*چرا:* پروندهٔ مدیران — پرامدترین ارزیابی‌های سازمان — با دو چشم بسته می‌شد، در
حالی که پروندهٔ یک کارشناس با چهار. حالا معاونت نمره می‌دهد و ثبت می‌کند، منابع
انسانی بررسی می‌کند، و بعد مدیرعامل. مرحلهٔ تأیید معاونت پریده می‌شود چون *انجام
شده*، نه چون وجود ندارد.

**صندلیِ تکراری در زنجیره**
هیچ‌جا سنجیده نمی‌شد که سه مرحلهٔ زنجیره سه *نفر* باشند، پس یک نفر می‌توانست دو
صندلی بگیرد و گزارش رویدادها *دو تأیید* نشان بدهد — دو رویداد، دو مُهر، یک آدم.
حالا ترکیب‌هایی که بیانِ درست‌تری دارند رد می‌شوند و پیام خطا همان بیان را
می‌گوید. «مسئول واحد = مدیرعامل» عمداً مجاز مانده، چون تنها راه ثبتِ کسی است که
مستقیم زیر نظر مدیرعامل کار می‌کند — ولی سند نهایی صریحاً می‌گوید نمره‌دهنده و
تأییدکنندهٔ نهایی یک نفر بوده‌اند.
*چرا:* ممنوع‌کردنِ آن حالت یعنی آن افراد قابل ثبت نیستند — همان اشتباهی که یک بار
با اجباری بودنِ ستون معاونت مرتکب شدیم.

**کارمند منابع انسانی و پروندهٔ خودش**
سه مرحلهٔ زنجیره گارد داشتند، مرحلهٔ منابع انسانی نه — و آن تنها مرحله‌ای است که
صاحبِ از پیش تعیین‌شده ندارد و از صف مشترک برداشته می‌شود. یعنی کارمند HR
می‌توانست پروندهٔ خودش را تحویل بگیرد، تأیید کند، لغو کند، یا اعتراض خودش را رد
کند. حالا هیچ‌کدام، و حتی دیدنِ پرونده از پنل HR هم نه.

**پوشش دوره دیگر دروغ نمی‌گوید**
مخرجِ «واجدان ارزیابی» فقط پرسنلی را می‌شمرد که زنجیره داشتند، پس کسی که
زنجیره‌اش تنظیم نشده بود از آمار *حذف* می‌شد و پوشش می‌توانست ۱۰۰٪ نشان بدهد در
حالی که ده نفر ارزیابی نشده بودند. حالا مخرج همهٔ پرسنل فعال است و بی‌زنجیره‌ها
یک شکافِ دیده‌شدنی‌اند.

**برنامهٔ بهبود برای نتایج زیر ۶۰٪**
واجد بودن با مقایسهٔ *رشتهٔ* نتیجه سنجیده می‌شد و فقط «تمدید مشروط» را می‌پذیرفت.
یعنی بدترین عملکردها — همان‌هایی که پیش از قطع همکاری بیشتر از همه به سابقهٔ
مکتوب نیاز دارند — هیچ مسیر مستندسازی نداشتند. حالا با عدد سنجیده می‌شود و سقفش
در طرح نمره‌دهی نسخه‌دار است.
*چرا (نیمهٔ دومش):* برچسب‌ها از قبل در خودِ طرح تعریف می‌شدند، پس سازمانی که
برچسب خودش را می‌نوشت، آن مقایسهٔ رشته‌ای هیچ‌وقت جواب نمی‌داد و برنامهٔ بهبود
بی‌صدا برای *هیچ‌کس* فعال نمی‌شد.

**بستنِ دوره‌ای که پروندهٔ باز دارد**
تا امروز بی‌صدا انجام می‌شد: وضعیت عوض می‌شد و آن پرونده‌ها همان‌طور در گردش‌کار
می‌ماندند تا ماه‌ها بعد زیر یک دورهٔ بسته نهایی شوند. حالا تأیید صریح می‌خواهد و
تعداد پرونده‌های باز در همان لحظه در گزارش رویدادها ثبت می‌شود.

---

### ظاهر

**انتخاب تم روی صفحهٔ ورود**
تا امروز کلید تم فقط داخل برنامه بود، یعنی کسی که تم شب را می‌خواست اول باید
صفحهٔ روشنِ ورود را تحمل می‌کرد. همان انتخاب ذخیره می‌شود و پس از ورود هم برجاست.

**فاصلهٔ کفِ پنجره‌ها**
پنجره‌هایی که دکمهٔ پایین ندارند — «دربارهٔ پلتفرم» و «تغییرات نسخه‌ها» — آخرین
سطرشان به لبهٔ کارت می‌چسبید. فاصله به خودِ کامپوننت مشترک اضافه شد، پس هر پنجرهٔ
بی‌دکمهٔ دیگری هم درست شد.

---

## v0.4.0 — شهریور ۱۴۰۵

### راه‌اندازی سازمان روی دیتابیس خام

**نخستین حساب، بدون مرغ و تخم‌مرغ**
اسکریپت ساخت حساب مدیر سامانه (`create_admin.py`) روی دیتابیس خالی اجرا می‌شود و
همان یک حساب، بقیهٔ سازمان را می‌سازد یا وارد می‌کند. رمز هرگز به‌صورت آرگومان خط
فرمان گرفته نمی‌شود.
*چرا:* بدون آن، هیچ راهی برای رسیدن به اولین ورود وجود نداشت — سامانه‌ای که فقط
با داده‌های دمو راه می‌افتاد، در عمل قابل نصب نبود.

**بارگذاری اکسل، سازمان را می‌سازد نه فقط فهرست را**
یک فایل، چهار چیز: ردیف پرسنل، حساب کاربری، زنجیرهٔ ارزیابی (مسئول مستقیم /
معاونت / مدیرعامل)، و نام کاربری و رمز اولیهٔ دلخواه از داخل همان فایل.
*چرا:* پیش از این، پس از وارد کردن ۴۲ نفر، هیچ‌کدام قابل ارزیابی نبودند تا وقتی
زنجیرهٔ هرکدام دستی ساخته می‌شد — و رمزهای تولیدی سامانه فقط یک بار دیده می‌شدند
و بس.

**زنجیرهٔ ارزیابی می‌تواند معاونت نداشته باشد**
در ساختار واقعی سازمان، عده‌ای مستقیم زیر نظر مدیرعامل‌اند. ستون دیگر اجباری
نیست و پرونده از منابع انسانی مستقیم به مدیرعامل می‌رود.
*چرا:* تنها راه ثبت این افراد، گذاشتن یک معاونتِ ساختگی بالای سرشان بود — و همان
نام ساختگی بعداً پای تأیید پروندهٔ آن‌ها در سندی می‌نشست که امضا می‌شود.

---

### نتیجهٔ ارزیابی

**امتیاز ویژه: کاری که در هیچ شاخصی نمی‌گنجد**
ارزیاب می‌تواند بابت کارِ خارج از شرح وظایف، امتیازی اختیاری (تا سقفِ نسخهٔ طرح)
به نمرهٔ نهایی اضافه کند. دلیلش اجباری است — در سطح دیتابیس، نه فقط در فرم — و
سند نهایی امتیازِ فرم و امتیاز ویژه را جدا از هم چاپ می‌کند.
*چرا:* تا امروز ارزیاب یا چنین کاری را نادیده می‌گرفت، یا نمرهٔ یک شاخصِ بی‌ربط را
بالا می‌برد تا جبرانش کند. یعنی نبودِ این بخش، دادهٔ خودِ شاخص‌ها را هم آلوده
می‌کرد.

**تفکیک دفتر مرکزی از کارخانه در گزارش‌ها**
میانگین‌ها و نمودارها بر اساس محل قابل تفکیک شدند.
*چرا:* میانگینِ دو محیط کاری متفاوت، عددی است که هیچ‌کدام را توصیف نمی‌کند.

---

### پرسنل و حساب‌ها

**تکلیف کارمندی که از سازمان می‌رود**
تاریخ و علت خروج (استعفا، اخراج، پایان قرارداد، بازنشستگی) ثبت می‌شود، حسابش
بسته می‌شود و پرونده‌های بازش تعیین تکلیف می‌شوند.
*چرا:* «غیرفعال» به‌تنهایی این را نمی‌گفت، و استعفا با پایان قرارداد در هیچ
گزارشی یک چیز نیست.

**حساب کاربری برای پرسنلی که از قبل در سامانه است**
تا امروز فقط هنگام بارگذاری اکسل حساب ساخته می‌شد؛ حالا از خود صفحهٔ پرسنل هم
می‌شود.

**نام واقعی روی حساب‌ها**
همه‌جا به‌جای نام کاربری، نام فرد دیده می‌شود.

---

### اختیارات و تنظیمات

**اختیارات مدیر سامانه از منابع انسانی جدا شد**
«گزارش رویداد» و «مدیریت سامانه» از پنل منابع انسانی کامل برداشته شدند و فقط در
پنل پشتیبانی می‌مانند.
*چرا:* دسترسی به کامل‌ترین ردِ تصمیم‌ها نباید به کسی گره بخورد که خودش در زنجیرهٔ
همان تصمیم‌ها می‌ایستد.

**تنظیم ایمیل و پیامک از داخل پنل**
همراه با ارسال پیام آزمایشی. کلیدها و رمزها همچنان فقط در فایل تنظیمات سرور
می‌مانند و هرگز در دیتابیس ذخیره یا از API برگردانده نمی‌شوند.

---

## v0.3.0 — مرداد ۱۴۰۵

### امنیت و دسترسی

**جداسازی «کسی که سامانه را می‌چرخاند» از «کسی که ارزیابی می‌کند»** · `P0-03`
حساب «پشتیبانی فنی» ساخته شد که سامانه را نگه می‌دارد، بخش‌ها را روشن و خاموش
می‌کند، و **به هیچ پروندهٔ ارزیابی دسترسی ندارد**. «مدیر سامانه» هم دیگر یک نقش
همه‌کاره نیست: پنج مجوز جدا که به هر حسابی قابل دادن است.
*چرا:* تا امروز اگر نتیجه‌ای زیر سؤال می‌رفت، نمی‌شد ثابت کرد کسی که تصمیم گرفته
همان کسی نبوده که قاعده را نوشته.

**سامانه می‌گوید تفکیک وظایف واقعاً برقرار هست یا نه**
صفحهٔ «مدیریت سامانه» بالای خودش وضعیت را می‌نویسد: کدام حساب‌ها هم پرونده تأیید
می‌کنند و هم قاعده می‌نویسند، و راه دقیق خارج شدن از این وضع.
*چرا:* سازوکار ساخته شده بود ولی خاموش مانده بود — از بیرون «انجام‌شده» به‌نظر
می‌رسید در حالی که در عمل هنوز همان یک نفر هم تصمیم می‌گرفت و هم قاعده می‌نوشت.
سازوکارِ خاموش از نداشتنش بدتر است، چون اطمینان را می‌دهد بی‌آنکه خاصیتش را بدهد.

**یک حفرهٔ دسترسی که وجود داشت و کشف شد**
فهرست پرونده‌ها بر اساس نقش فیلتر می‌شد و انتهایش نوشته بود «منابع انسانی همه را
می‌بیند» — یعنی **هر نقشی که در فهرست نبود، همهٔ پرونده‌های سازمان را می‌دید**.
حالا برعکس است: فقط منابع انسانی صراحتاً همه را می‌بیند و هر نقش ناشناخته هیچ.
*چرا:* حفره از قبل بود، ولی تا وقتی همهٔ نقش‌ها «خودی» بودند دیده نمی‌شد.

**«گزارش رویدادها» یک صفحه ماند، ولی دو دامنهٔ دید پیدا کرد**
پشتیبانی فنی برای عیب‌یابی به آن نیاز دارد، ولی بخشی از همین رویدادها عیناً نمرهٔ
افراد را در خود دارند. به‌جای جابه‌جا کردن صفحه، خودِ داده تفکیک شد: پشتیبانی فقط
ورود و خروج، قفل حساب و تغییر مجوز را می‌بیند — بدون هیچ ردی از محتوای پرونده‌ها.

**دفاع در برابر حدس رمز عبور** · `P0-04`
قفل حساب پس از تلاش‌های ناموفق، و شناسایی درست نشانی کاربر (که قبلاً قابل جعل بود).

**ورود دوعاملی؟ هنوز نه — ولی نشست‌ها قابل مدیریت شدند** · `P2-06`
هر کاربر می‌بیند از چه دستگاه‌هایی وارد شده و می‌تواند هرکدام را ببندد.

**خودارزیابی برای خودِ فرد ممنوع شد**
کسی نمی‌تواند ارزیابی‌کنندهٔ پروندهٔ خودش باشد.

**نشتی احتمالی از فایل‌های Excel بسته شد** · `P2-02`
متنی که کاربر وارد می‌کند، دیگر نمی‌تواند در Excel به فرمول تبدیل شود.

---

### درستی نتیجه و پایداری تاریخ

**قواعد نمره‌دهی نسخه‌دار شد** · `P1-04`
وزن بخش‌ها، جدول آستانه‌ها و قاعدهٔ شواهد از کد بیرون آمدند و به دیتابیس رفتند —
با نسخه‌گذاری. هر پرونده به نسخه‌ای که زیر آن باز شده مهر می‌خورد.
*چرا:* بدون این، عوض کردن یک وزن، معنای هر ارزیابی گذشته را بی‌صدا بازنویسی
می‌کرد: پرونده‌ای که پارسال «تمدید با شرایط استاندارد» گرفته بود، امروز ممکن بود
«عدم تمدید» نشان بدهد بدون اینکه هیچ نمره‌ای عوض شده باشد. برای فروش به مشتری
دوم هم لازم بود — هر سازمان قواعد خودش را دارد.

**سؤال‌های فرم هم نسخه‌دار شدند — و یک خرابی روزمره بسته شد** · `P1-05`
هر پرونده به نسخه‌ای که زیر آن باز شده گره می‌خورد.
*چرا:* تا امروز، افزودن یا برداشتن یک شاخص وسط چرخه، **هر پیش‌نویس در جریان را
قفل می‌کرد**. ارزیاب فرم را کامل پر می‌کرد و می‌رفت؛ منابع انسانی سؤالی اضافه
می‌کرد؛ فردا دکمهٔ «ثبت» کار نمی‌کرد و پیام می‌داد که به سؤالی نمره نداده‌اید که
آن روز اصلاً وجود نداشت. هیچ‌کس هم به منابع انسانی نگفته بود که با یک ویرایش،
دوازده پرونده را قفل کرده است. حالا منابع انسانی پیش از کلیک می‌بیند تغییرش روی
چند پروندهٔ باز اثر می‌گذارد.

**ویرایش متن یک شاخص: سامانه دیگر حدس نمی‌زند**
اگر شاخصی قبلاً نمره خورده باشد، هنگام ویرایش پرسیده می‌شود: «اصلاح نگارشی است یا
معنای سؤال عوض می‌شود؟» اولی درجا انجام می‌شود و دلیلش ثبت؛ دومی شاخص تازه می‌سازد
و قبلی را بایگانی می‌کند.
*چرا:* سامانه نمی‌تواند غلط املایی را از «سؤال را عوض کردم» تشخیص بدهد، ولی آدمی
که تایپ می‌کند می‌تواند. بدون این، نموداری که «شاخص ۷» را در دو سال مقایسه می‌کرد،
ممکن بود دو سؤال کاملاً متفاوت را کنار هم گذاشته باشد.

**سند رسمی، غیرقابل انکار** · `P1-09`
گزارش رویدادها زنجیرهٔ هش دارد؛ یعنی اگر کسی ردیفی را دست بزند یا حذف کند، معلوم
می‌شود. دو مسیری هم که اصلاً ثبت نمی‌شدند، حالا ثبت می‌شوند.

**امتیازهای پیش‌نویس دیگر گم نمی‌شوند**
باگی که با برگشت پرونده و بدون رفرش، کار نیمه‌تمام ارزیاب را پاک می‌کرد.

**نوشتن هم‌زمان روی یک پرونده امن شد** · `P0-05`
دو نفر که هم‌زمان روی یک پرونده اقدام کنند، دیگر نتیجه‌ای نمی‌سازند که هیچ‌کدام
نخواسته بودند.

---

### حریم خصوصی

**آمارهای گروه‌های کوچک پنهان می‌شوند** · `P1-08`
اگر فیلتر کردن یک گزارش به گروهی با تعداد کم برسد، به‌جای عدد، «محرمانه» نشان داده
می‌شود.
*چرا:* میانگین یک واحد سه‌نفره، عملاً نمرهٔ آن سه نفر است. بدون این، تحلیل به یک
راه دور زدن دسترسی تبدیل می‌شد. این پیش‌شرطِ باز کردن گزارش‌ها برای مدیران بود.

---

### صدای کارمند

**کارمند در فرایندی که دربارهٔ خودش است، جایگاه پیدا کرد** · `P0-06`
دیدن نتیجهٔ نهایی، خودارزیابی پیش از دیدن نمرهٔ ارزیاب، مسیر رسمی اعتراض، و دانلود
کارنامهٔ رسمی خودش.

**دکمهٔ «رؤیت شد» عوض شد به «نتیجه را دیدم»**
پنجرهٔ تأیید حالا صریحاً می‌نویسد که **مشاهده به معنی پذیرش نیست** و راه اعتراض
دقیقاً پس از همین ثبت باز می‌شود. راه اعتراض هم پیش از آن اعلام می‌شود.
*چرا:* «رؤیت» در گفتار اداری یعنی «دیدم»، ولی خیلی‌ها آن را «قبول دارم» می‌خوانند.
نتیجه‌اش این بود که کارمند یا فکر می‌کرد دارد نتیجه را تأیید می‌کند، یا از ترسِ از
دست دادن حق اعتراض اصلاً کلیک نمی‌کرد و پرونده معلق می‌ماند.

**«پیشنهاد سامانه» شد «بر اساس جدول مصوب سازمان»**
*چرا:* جملهٔ قبلی مثل حکم یک ماشین دربارهٔ آیندهٔ شغلی خواننده خوانده می‌شد، در
حالی که خروجی جدول آستانه‌هایی است که خودِ سازمان تصویب کرده.

**فرم خودارزیابی دیگر کار نیمه‌تمام را از دست نمی‌دهد**
پیش‌نویس در مرورگر خودِ فرد ذخیره می‌شود (نه روی سرور — پیش‌نویسِ ثبت‌نشده هنوز
حرفِ کسی نیست)، و ثبت نهایی یک‌بار پرسیده می‌شود.
*چرا:* فرم ارزیاب از قبل ذخیرهٔ خودکار داشت، فرمِ کارمند نداشت. یعنی کم‌قدرت‌ترین
آدمِ این فرایند، نابخشنده‌ترین فرم را داشت: بیست شاخص با یادداشت، که با یک کلید
«بازگشت» از بین می‌رفت.

**خودارزیابی و نمرهٔ ارزیاب حتماً به یک مجموعه سؤال پاسخ می‌دهند**
*چرا:* دو خرابی پیدا شد که هر دو در سمتِ کارمند بودند — همان سمتی که کمتر از همه
بررسی می‌شود. اول: اگر منابع انسانی وسط چرخه شاخصی را کنار می‌گذاشت، ثبت
خودارزیابی رد می‌شد. دوم و مهم‌تر: پرونده‌ای که کارمند خودارزیابی‌اش را در آن ثبت
کرده بود، «دست‌نخورده» حساب می‌شد و به مجموعهٔ تازه منتقل می‌شد — پس کارمند به
بیست سؤال جواب می‌داد و ارزیاب به نوزده سؤال نمره می‌داد، و آن مقایسه‌ای که کل
خودارزیابی برایش وجود دارد بی‌صدا ناقص می‌شد.

---

### گردش کار

**راه خروج از پروندهٔ گیرکرده** · `P0-02`
اگر تأییدکننده‌ای از سازمان برود، پرونده دیگر برای همیشه قفل نمی‌ماند: منابع انسانی
می‌تواند مسئول مرحله را عوض کند، پرونده را تحویل بگیرد، یا با ذکر دلیل لغوش کند.
*چرا:* تنها درمانِ قبلی، دست بردن مستقیم در دیتابیس بود.

**هر پرونده مسئول مشخص در منابع انسانی دارد**
تا پیش از این، سه مرحلهٔ دیگر صاحب داشتند ولی مرحلهٔ منابع انسانی نه — پس پاسخ
سؤال «مسئولش که بود؟» وجود نداشت، فقط «چه کسی کلیک کرد».

**یادآوری‌ها قابل اثبات اجرا می‌شوند** · `P0-08` `P1-02`
و ساعت درست را می‌سنجند: «چقدر در این مرحله مانده»، نه «سن کل پرونده».

**دلیل برگشت پرونده بالای فرم می‌آید**
*چرا:* قبلاً ارزیاب یک نشان کوچک «برگشتی» می‌دید و باید تا ته صفحه اسکرول می‌کرد
تا دلیلش را بین کامنت‌ها پیدا کند — مهم‌ترین چیزی که باید می‌خواند، کم‌دیده‌ترین
چیز صفحه بود.

**نوار مسیر چهارمرحله‌ای**
روی پرونده و کارت کارمند: چه چیزی تمام شده، الان کجاست، چقدر مانده.
*چرا:* نام مرحلهٔ فعلی همه‌جا نوشته می‌شد، ولی هیچ‌کس نمی‌دید کجای کل مسیر است.

**دوره‌های ارزیابی فعال شدند** · `P1-07` `P2-03`
و می‌شود یک چرخهٔ کامل را برای یک گروه یک‌جا باز کرد، با پیش‌نمایش قبل از اجرا.

**برنامه‌های بهبود قابل استفاده توسط مسئول پیگیری شدند** · `P1-10`
قبلاً فقط منابع انسانی دسترسی داشت؛ مسئول پیگیری اعلان می‌گرفت ولی راهی برای باز
کردن برنامه‌ای که به خودش سپرده شده بود نداشت.

---

### اعلان‌ها

**زیرساخت اعلان بیرونی (ایمیل و پیامک)** · `P1-03`
صف تحویل با تلاش مجدد، ارجحیت هر کاربر، و جداسازی «شمارهٔ غلط» از «شبکه قطع بود».
وصل‌کردن هر سرویس ایمیل یا پیامکی حالا چند خط تنظیمات است، نه تغییر کد.
*پیش‌فرض خاموش:* تا کسی سرویسی تعریف نکند، هیچ چیز بیرون نمی‌رود.

---

### گزارش و تحلیل

**مدیران و مسئولان تحلیل خودشان را دارند** · `P2-01`
«الگوی نمره‌دهی من» برای ارزیابان، و «تحلیل سازمان» برای مدیران ارشد.

**نمودارها یک زبان بصری پیدا کردند**
نمودار میله‌ای بیست‌تایی جایش را به نمودار خوانا داد؛ برچسب‌های روی هم افتاده،
روند تک‌نقطه‌ای و ستون‌های بی‌قواره درست شدند. خروجی PNG نمودارها هم اصلاح شد.

**جزئیات گزارش رویدادها به فارسی خوانا شد**
به‌جای متن خام فنی.

---

### تجربهٔ کاربری روزمره

**پیام‌های خطا دیگر فرار نمی‌کنند**
قبلاً چهار ثانیه بعد خودشان می‌رفتند — حتی وقتی می‌گفتند دقیقاً کدام شاخص مشکل
دارد. حالا تا وقتی کاربر نبندد می‌مانند. رنگشان هم دیگر قرمزِ برند نیست، چون قرمزی
که رنگ هر دکمهٔ اصلی است، خبری نمی‌دهد.

**کارهای بازگشت‌ناپذیر ظاهر متفاوتی گرفتند**
پنج کارِ واقعاً برگشت‌ناپذیر (لغو پرونده، صدور سند نهایی، لغو برنامهٔ بهبود، بستن
دوره، فعال‌سازی طرح نمره‌دهی) نام فرد و آنچه برنمی‌گردد را می‌نویسند، و مکان‌نمای
صفحه‌کلید روی «انصراف» می‌نشیند نه «تأیید».
*چرا:* پنجرهٔ تأیید برای «مشاهده کردم» و «لغو پرونده» دقیقاً یک شکل بود. بعد از
بیست تأییدِ بی‌ضرر، بیست‌ویکمی هم خودکار زده می‌شود.

**نام‌هایی که به چیز اشتباهی اشاره می‌کردند**
«داشبورد مدیرعامل» → «صندوق تأیید نهایی» (چون یک صف بود نه داشبورد، و داشبورد
واقعی صفحهٔ دیگری است). «نمره‌دهی من» → «الگوی نمره‌دهی من» (چون می‌شد آن را
«نمره‌هایی که به من داده‌اند» خواند، یعنی دقیقاً برعکس).

**جست‌وجو در فهرست زیرمجموعه‌ها**
مسئول واحدی با شصت نفر زیرمجموعه، تا امروز باید اسکرول می‌کرد — در حالی که فهرست
ارزیابی‌ها درست پایین‌تر جست‌وجوی کامل داشت. نقطهٔ *شروع* کار، تنها جایی بود که
ابزار نداشت.

**نشانی صفحه، تب انتخاب‌شده را به یاد می‌آورد**
تحلیلگری که «گزارش‌های تحلیلی» را باز کرده و لینک را می‌فرستد، دیگر طرف مقابل روی
«نمای کلی» نمی‌افتد.

**دکمهٔ «بازگشت» دیگر کاربر را از برنامه بیرون نمی‌اندازد**
کسی که از لینک داخل اعلان مستقیم وارد شده بود، با «بازگشت» از محصول خارج می‌شد.

**تعداد نمایش در هر صفحه قابل تنظیم شد**
پیش‌فرض ۱۰، ولی می‌شود ۲۰ یا ۵۰ یا ۱۰۰ کرد.

**ورود گروهی پرسنل از Excel**
سه مرحله: فایل → پیش‌نمایش با خطای هر ردیف → نتیجه، به‌همراه فایل رمزهای موقت که
فقط یک‌بار قابل دانلود است. حساب کاربری کارمند در همان مرحلهٔ ساخت پرسنل ایجاد
می‌شود.

**فرم رمز عبور آدم‌وارتر شد**
نمایش رمز، تیکِ هر قاعده به‌صورت زنده، و تولید رمز پیشنهادی.

---

### دسترس‌پذیری

**حلقهٔ فوکوس صفحه‌کلید دیده می‌شود**
کنتراستش ۲٫۱۳ به ۱ بود (زیر حداقل استاندارد ۳ به ۱)؛ حالا ۴٫۰۶ به ۱.

**Escape پنل اعلان‌ها را می‌بندد**
قبلاً کاربر صفحه‌کلید هیچ راهی برای بستنش نداشت جز رد شدن از تمام اعلان‌ها.

**شمارنده‌های متحرک، تنظیم «حرکت کمتر» را رعایت می‌کنند**
کسی که این گزینه را به‌خاطر حساسیت روشن کرده بود، بدترین حالت را می‌دید: صفحهٔ
آرام و فقط عددهای در حال دویدن.

**فوکوس صفحه‌کلید در پنجره‌های تأیید درست شد**
در **هیچ** پنجره‌ای فوکوس روی دکمهٔ درست نمی‌نشست؛ دو بخش از کد سر آن رقابت داشتند
و برنده دکمهٔ «بستن» بود.

---

### ظاهر و هویت

**تم «شب».** سرمه‌ای، نه مشکی — با گزینهٔ «مثل سیستم» به‌عنوان پیش‌فرض، چون کسی
که کل دستگاهش را تیره کرده انتظار ندارد این یکی برنامه روشن باز شود. تم پیش از
اولین رنگ‌آمیزی صفحه تعیین می‌شود تا لحظه‌ای صفحهٔ سفید دیده نشود.

**فوتر و لوگوی توسعه‌دهنده.** «Developed by DbsStudio» با پیوند به سایت، و لوگوی
واقعی به‌جای نشان جایگزین.
*ضمناً:* نشانی لوگو نسبی بود و از هر صفحهٔ داخلی ۴۰۴ می‌گرفت — یعنی لوگوی واقعی
هیچ‌وقت دیده نشده بود و همیشه نشانِ جایگزین رندر می‌شد. حالا دو فایل دارد، یکی
برای هر تم.

**«دربارهٔ پلتفرم» و «تغییرات نسخه‌ها» روی صفحهٔ ورود.** پشت دکمه، نه پیش‌فرض —
صفحهٔ ورود باید یک کار داشته باشد. متنشان جداگانه برای مخاطب *عمومی* نوشته شده و
از روی همین فایل ساخته نشده: صفحهٔ ورود پیش از احراز هویت است، پس تاریخچهٔ
اشکالات امنیتی آن‌جا جایی ندارد.

**شمارهٔ نسخه یک منبع پیدا کرد.** پیش از این در دو جا نوشته شده بود با یادداشتی
که «همگام نگهشان دارید» — و چیزی که باید دستی همگام بماند، نمی‌ماند.

### موبایل و ظاهر

**قابل نصب روی موبایل** · `P2-04`
و فرم امتیازدهی روی گوشی بازطراحی شد — نه فقط کوچک‌تر: جدول و اسلایدر جایشان را به
دکمه‌های مجزا دادند.

**آیکن و رنگ اپلیکیشن با برند هماهنگ شد**
آیکن نصب‌شده روی گوشی هنوز فیروزه‌ای/بنفشِ قالب اولیه بود — رنگی که در هیچ‌جای
محصول دیده نمی‌شود.

---

### پایداری و نگهداری

**اسناد PDF از مسیر تأیید نهایی بیرون رفتند** · `P2-05`
تأیید نهایی دیگر منتظر ساخته شدن سند نمی‌ماند. اندازهٔ متن‌های آزاد و ظرفیت
اتصال‌های دیتابیس هم مشخص شد.

**سلامت سامانه قابل مشاهده شد** · `P1-12`
معیارهای عملکرد، و بررسی آمادگی که واقعاً می‌تواند «آماده نیستم» بگوید.

**بخش‌های سامانه قابل روشن و خاموش شدن‌اند**
شش بخش، از دیتابیس نه از کد. خاموش‌کردن هیچ داده‌ای را پاک نمی‌کند؛ فقط از منو
برداشته می‌شود و ثبت تازه در آن بسته می‌شود.

**آماده‌سازی استقرار** · `P0-07` `P0-09`
مهاجرت دیتابیس از راه‌اندازی سرویس جدا شد، سرویس با کاربر غیر-root اجرا می‌شود، و
نمونهٔ تنظیمات محیط واقعی مستند شد. دادهٔ نمایشی هم پشت یک کلید صریح رفت تا به‌طور
تصادفی روی محیط واقعی ساخته نشود.

---

## یافته‌های تازه‌ای که در حین کار کشف شدند

این‌ها در فهرست ممیزی نبودند و هنگام انجام کارهای بالا پیدا شدند:

- **حفرهٔ دسترسی در فهرست پرونده‌ها** — بالا توضیح داده شد. مهم‌ترینشان.
- **سازوکار تفکیک وظایف ساخته شده بود ولی خاموش مانده بود** — بالا توضیح داده شد.
- **فوکوس صفحه‌کلید در هیچ پنجرهٔ تأییدی درست نمی‌نشست** — بالا.
- **آیکن اپلیکیشن موبایل با برند نمی‌خواند** — بالا.
- **ادعای ممیزی دربارهٔ اجباری‌نبودن دلیلِ برگشت پرونده غلط بود** — بررسی شد؛ دلیل
  از قبل اجباری بوده و ثبت می‌شده. فقط جای نمایشش مشکل داشت.
- **جعبهٔ جست‌وجو در چهار صفحه کپی شده بود با چهار عرض متفاوت** — هیچ‌کدام تصمیم
  نبودند، فقط جا مانده بودند. یکی شد.
- **صفحهٔ شاخص‌ها اصلاً امکان ویرایش متن نداشت** — یک غلط املایی فقط با حذف و ساخت
  دوباره درست می‌شد، که تاریخچهٔ همان شاخص را هم پاک می‌کرد.
- **پروندهٔ دارای خودارزیابی، «دست‌نخورده» حساب می‌شد** — بالا توضیح داده شد. این
  یکی از دو خرابی‌ای بود که در کار خودمان جا مانده بود.
- **جعبهٔ جست‌وجوی صفحهٔ شاخص‌ها یک تابع را اشتباه نام‌گذاری کرده بود** — به‌شکلی که
  ابزار بررسی کد ردش می‌کرد. کشف شد چون بار اول آن ابزار اجرا نشده بود.
# v0.9.5 — شهریور ۱۴۰۵

## تجربهٔ کاربری و مدیریت دسترسی

- داشبورد منابع انسانی فشرده‌تر شد و جدول‌ها، فیلترها و انتخاب تاریخ رفتار یکدست‌تری دارند.
- بخش‌های مدیریت سامانه و مدیریت حساب/پرسنل دسته‌بندی و تب‌بندی شدند.
- نمای کارمند، خودارزیابی، اعتراض، نتیجه و تأیید مشاهدهٔ نتیجه از پنل مدیریت قابل تنظیم‌اند.
- حساب HR جدید فقط مجوزهای حساب کاربری، پرسنل/زنجیرهٔ ارزیابی و شاخص‌ها/طرح نمره‌دهی را می‌گیرد؛ اختیارهای سیستمی پیش‌فرض خاموش‌اند.

---
