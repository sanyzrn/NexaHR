backend/.venv/
backend/__pycache__/
backend/**/__pycache__/
backend/.env
# فایل .env کنار docker-compose.yml (کپی‌شده از .env.production.example) رازهای
# واقعی استقرار را دارد — نمونه‌های *.example عمداً استثنا شده‌اند.
.env
!*.example
backend/*.egg-info/
backend/.pytest_cache/
frontend/node_modules/
frontend/dist/
.DS_Store
*.pyc

# فایلِ اکسلِ نمونه که setup_e2e.py می‌سازد — ساختنی است، نه دارایی مخزن.
e2e/e2e-personnel.xlsx

# گزارشی که راه‌انداز فقط وقتی می‌نویسد که خودش پیش از باز شدنِ پنجره بمیرد.
launcher-error.log
tools/**/__pycache__/

# نسخه‌های پشتیبانِ دیتابیس که راه‌انداز می‌گیرد — دادهٔ واقعیِ سازمان، و
# دقیقاً همان چیزی که هیچ‌وقت نباید در مخزن بنشیند.
backups/
